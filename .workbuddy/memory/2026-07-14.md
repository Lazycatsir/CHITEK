# 2026-07-14 工作日志

## 产品系列四张卡片「弹出加载」动画（可反复触发）
- 文件：`src/pages/en/products.astro`（`#product-series` 区域，AHF/SVG 两套面板各 4 张卡片）
- 实现：
  - CSS：`@keyframes psPopIn`（scale 0.82→1.03→1 + translateY + opacity），`.ps-card{opacity:0}` 默认隐藏，`.ps-card.popped` 跑动画（`both` 填充），`prefers-reduced-motion` 下直接显示。
  - JS：给两套面板的 `[data-product-track] > div` 打 `.ps-card`；`popSeriesCards()` 用「移除 .popped → 强制回流 → 重加 .popped + 阶梯 delay(i*90ms)」实现重播。
  - 触发点（可反复触发）：① IntersectionObserver 进入视口弹一次、离开后再次进入重弹（沿用 HMI add/remove 思路）；② 点 `.sticky-hero-tab` 切 AHF/SVG 时 `requestAnimationFrame` 重弹当前面板。
- 验证：dev(4321) 200；Playwright CDP 实测 4 张卡片 load/SVG切换/切回/滚动重入 均 `popped:True`、opacity 1。无 JS 时卡片保持可见（优雅降级）。
- 注：仅改了 en 语言；zh/es/ar/pt 未做（按 EN first 规则，待用户确认是否推广）。

## AHF 英文产品族页布局优化（参考 CoEpower parallel-rack AHF）
- 文件：`src/pages/en/products/active-harmonic-filter.astro`（17:47 创建，后全量重写优化）
- 优化内容（融合参考页有效模式，原内容与 3 个 JSON-LD 全部保留）：
  - 粘性子导航 `sticky top-[72px] z-[999]`，9 个锚点链接 + IntersectionObserver 高亮当前节（脚本被 Astro 打包为 `?astro&type=script` 引用，已验证）。
  - Breadcrumb 下插入「6 卡参数速览」行（Rated Voltage/Capacity/THDi/Response/Efficiency/Topology）。
  - 章节编号 `01 / OVERVIEW` … `09 / FAQ`，每节 `scroll-mt-[150px]` 避免被固定导航遮挡。
  - Overview 加 Category + tags 药丸行；5 个 Series 卡加内联 tag 药丸。
  - 新增「Why CHITEK」信任区（4 卡：2700+ Units Delivered / IEC Certified Quality / 30+ Countries Served / OEM Fast Delivery）。
  - CTA 加 In stock·Fast dispatch、OEM/ODM customization 徽章；内联 `<style>`（隐藏滚动条）+ `<script>`（子导航高亮）。
- ⚠️ 提示："Why CHITEK" 数据（2700+ 台、30+ 国、2019 起）是从项目上下文推断，非用户明确确认，待用户核实数值。
- ⚠️ 待办：相关链接指向 `/products/static-var-generator`、`/products/sic-ahf-svg` 当前 404（这两族页未建，按 EN first 暂缓）；zh/es/ar/pt 同步未做；`npm run build` 受 dist 删除守卫限制需用户本地验证。
- 验证：dev(4321) 该页 HTTP 200，无编译错误；各区块/脚本/JSON-LD 均渲染。

## 去掉 products 页面的 Automatic Voltage Regulator (AVR)
- 需求：用户「去掉products页面的automatic voltage regulator」→ 五语 products 页面移除 AVR。
- 脚本 `remove_avr.py`（Python 正则，CRLF 感知 `\r?\n`，保留 ar BOM）：对 5 个文件各移除 ① sticky hero tab 按钮 `data-tab="2"`、② hero「Coming Soon」`<section data-tab="2">` 整块（含 `<!-- Tab 2: AVR -->` 注释与下一代 AVR 段落）、③ `matrixTitles` 第 3 项、④ `productHashMap` 的 `'avr':'2'`；并对 zh/en/es/pt 清理 meta title/description 中的 AVR（ar meta 原本就无 AVR）。
- 关键发现：仅改 products.astro 后，dev 返回的 HTML 仍含 AVR —— 因全局 `BaseLayout.astro` 的 JSON-LD（Organization + WebSite 两条 `description`）硬编码了 AVR。故同步改 `BaseLayout.astro` 第 65、81 行去掉 AVR（**全局影响所有页面的结构化数据**；已向用户说明，若只想要 products.astro 改动可回退）。
- 验证（Playwright CDP，共享 venv）：
  - 5 语 `/products`、`/en/products`、`/es/products`、`/pt/products`、`/ar/products` 均 `AVR_in_html=False`、`.sticky-hero-tab` 数量=2（AHF/SVG）、`panel_data_tabs` 仅 0/1、`#matrix-title` 点 tab0/tab1 后均正确、`tab_switch_ok=True`。
  - 控制台：仅 zh 首次跑出现 1 个 404（"Failed to load resource 404"），隔离复跑 zh/en 均**无 4xx/失败请求** → 判定为间歇性/与改动无关；其余均为项目已知良性全局 `PAGEERROR: Object`（第三方抛错，非本次引入）。
- 结论：AVR 已从 products 页面（含其 JSON-LD）彻底移除，五语一致，tab 切换/矩阵标题正常，无新增错误。
- 未做（超出范围，待用户决定）：首页 `index.astro` 等 meta 仍含 AVR；services/solutions 页有「AVR 选型指南」区块（独立页面，未动）。

## 英语 products 区域排序 + psPopIn 动画 同步到 zh/es/pt/ar
- 需求：用户「英语 /en/products 的区域内容排序同步到其它语言，比如 Four Product Series 放在 hero 下面，以及四张卡片有动画加载」。
- 关键发现：EN 顺序 = hero → product-series → features → value-prop → matrix；zh/es/pt/ar 顺序 = hero → value-prop → features → product-series → matrix。故「Four Product Series 置于 hero 下」= 把 value-prop 与 features 整体移到 product-series 之后（保持 EN 内部顺序 features→value-prop）。
- psPopIn 动画（`.ps-card` CSS + `@keyframes psPopIn` + 自包含 JS block）原本仅 EN 有；其余四语缺失。
- 脚本 `sync_products_order.py`（CRLF 安全、保留 ar BOM、幂等）：
  - ① `(?s)<section id="value-prop".*?</section>\r?\n` + `<section id="features".*?</section>\r?\n` 抽取两块，删后插入到 `<section id="product-series">...</section>` 的 `</section>` 之后（顺序 features→value-prop）；幂等守卫：value-prop 行号 > product-series 行号则跳过。
  - ② CSS 锚点 `  .reveal-x-left.visible { opacity: 1; transform: translateX(0); }` 后插入 psPopIn CSS。⚠️ 教训：曾用正则提取 EN 的 CSS 块失败——`@media (prefers-reduced-motion)` 在文件更靠前已出现、`@keyframes` 内嵌 `}`，导致 `.*?` 提前终止。改法：**CSS/JS 块改为逐行硬编码列表（join('\r\n')）**，不再正则提取。
  - ③ 唯一 `</script>` 前插入 psPopIn JS（同样硬编码）；守卫：`ps-card`/`seriesObserver` 已存在则跳过。
- 验证（Playwright CDP 共享 venv，5 语）：`order_ok=True`、4 张 `[data-product-track]>div` 卡均 `popped=True`、stagger=[0,90,180,270]ms、`new_errors=[]`（仅已知良性 `PAGEERROR: Object`）；tab 切换(active=1)/滚动重入 后 4 卡仍 `popped`。ar BOM 保留（head -c3 = efbbbf）。
- 产物：4 个 products.astro 已改；`.workbuddy/` 下 `sync_products_order.py` / `verify_products_sync.py` / `verify_products_sync.json`。
- 结论：四语 products 页现已与 EN 区域排序 + 卡片弹出动画完全一致，可反复触发，无新增错误。
