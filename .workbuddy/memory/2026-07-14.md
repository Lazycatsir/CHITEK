# 2026-07-14 工作日志（CHITEK）

## 英文首页 About 区微调（en/index.astro）
- ABOUT CHITEK 标题字号 32px→45px（原 1.4×）。
- Play Video 按钮 href 经核查本就指向 `/en/about`（about 页面存在、无 JS 拦截），未做改动。

## 英文产品页 Four Product Series 电流范围（en/products.astro）
- 仅改英文。AHF 面板（Tab 0）"Four Product Series, Full-Scenario Coverage" 区两张卡：
  - SiC 卡 APF Current：`50A ~ 100A` → `50A ~ 150A`（与 capacity 数组 C 系列上限 150A 一致）。
  - Anti-Pollution 卡 APF Current：`75A ~ 150A` → `50A ~ 200A`（与 A 系列数组 50A~200A 一致）。
- 注：SVG 面板（Tab 1）的 SiC/Anti-Pollution 卡显示 SVG Capacity(kVar)/Voltage，无 APF Current 安培行，未涉及。
- dev(4321) 验证：50A~150A×1、50A~200A×2（含原本 Standard 卡）、旧值均=0、/en/products 200。
- 未提交 git。

## 英文首页改动同步到 zh/es/ar/pt 首页
- 背景：早前"只调试英文"撤回了其它语言同步；本次用户要求把英文首页改动（en/index.astro）套到其余 4 语首页。
- 同步内容（与英文完全一致，纯结构同源，5 语 DOM 相同）：
  1. **Product Series 收敛动画**：grid wrapper 去掉 Tailwind `opacity-0 translate-y-6 ... fade-up`；左栏 `min-h-[240px] relative`→加 `fade-up conv-left`；中栏 `grid grid-cols-2 gap-px bg-border-light h-full`→加 `fade-up`；右栏 `flex items-center justify-end`→加 `fade-up conv-right`。
  2. **Solution Insights 动画**：标题 wrapper 去 Tailwind fade-up 加 `insight-title`；3 张卡片加 `insight-card`（错峰 120ms）。
  3. **CSS 块**（conv-left/right + insight-title/card + prefers-reduced-motion）注入到各文件 `</style>` 前。
  4. **重播观察器 JS**（revealEls: .conv-left/.conv-right/.insight-title/.insight-card，进视口加 .visible/出视口移除，threshold 0.12）注入 `</script>` 前。
  5. **本地化链接 lp()**：各文件 frontmatter 加 `isLocal` + `lp(prefix+p)`；Solution Insights 卡片 `/solutions#...`→`lp()`（全 4 语）；**仅 zh** 额外把裸 `/products`(×2) 与 HERO `/about`(×1) 包 lp()；es/ar/pt 的 `/es|ar|pt/products`、`/es|ar|pt/about` 本就带正确前缀，未动。
- 实现：`.tmp_sync_home_anim.py`（带断言+幂等，已移 /tmp/scratch-chitek-20260714/）。
- 验证：Playwright 无头（1280 宽）确认 es/zh 的 Products 收敛(.visible+transform 居中)、re-trigger(out→移除、回→重加)、Solutions 标题+三卡可见、控制台无报错；ar/pt 同样 reveal 正常、无报错（ar 为 RTL，水平收敛仍向中心，符合预期）。
- **zh HERO 轮播（用户既有）完好**（heroShow/heroStart 仍在），未触碰。
- 线上零改动：lp() 非本地时返回根相对，子域名架构正常。
- 未提交 git。

## 英文产品页 HMI 区「左右向中间靠拢」加载动画（en/products.astro）
- 仅改英文。HMI Section(id=hmi) 原为左文字栏+右图片栏各用通用 `.reveal`（淡入上移、只播一次）。
- 改为：左栏 `reveal`→`hmi-left`（起始 translateX(-64px)），右栏 `reveal`→`hmi-right`（起始 translateX(+64px)）；进入视口时两者 transform 收到 0 = 向中间靠拢。
- 新增独立 observer `hmiObserver`：进视口加 `.visible`、出视口移除 `.visible` → **可重复触发**（滚出再滚回重播）。threshold 0.25。
- CSS：`.hmi-left/.hmi-right{opacity:0; transition .7s}` + `.visible{opacity:1;translateX(0)}`；含 `prefers-reduced-motion` 兜底（直接显示）。
- 注意：Astro 把 `<script>` 打成外部 bundle，故内联 HTML grep 不到 hmiObserver，源文件 2032-2043 已确认存在。
- dev(4321) 验证：hmi-left/hmi-right 各 1 处 HTML 类、translateX(±64px) CSS 在、/en/products 200。
- 未提交 git。

## 英文产品页 Four Product Series 区 APF→AHF（en/products.astro）
- 仅改英文。用户要求该区 "APF" 全部改成 "AHF"（与全站 AHF 品牌一致）。
- 产品页 APF 仅存在于 AHF 标签的 Four Product Series 区，共 8 处：4× "APF Current"（Standard/SiC/Distributed/Anti-Pollution 卡电流行）+ 4× 子标题 "APF Flagship/Compact/Distributed/Heavy Industry"。replace_all 一次性改完。
- dev(4321) 验证：APF 剩余=0、AHF Current=4、/en/products 200。
- 未提交 git。

## 英文产品页三处改动同步到其它 4 语（zh/es/ar/pt）
- 背景：en/products.astro 当日改了 3 处（电流范围 / APF→AHF / HMI 左右靠拢动画），需同步 zh/es/ar/pt。
- 用带断言的 Python 脚本一次性同步（每文件断言关键串唯一，失败即停）：
  1. 电流范围：SiC `50A ~ 100A`→`50A ~ 150A`、Anti-Pollution `75A ~ 150A`→`50A ~ 200A`（数字串语言无关，replace_all）。
  2. APF→AHF：4 语产品页 APF 仅出现在 Four Product Series 区，replace_all 安全（zh「APF电流/APF旗舰…」、es/pt「Corriente/Corrente APF」、ar「APF Current」；子标题同步改）。
  3. HMI 动画：HMI 左栏 `reveal`→`hmi-left`、右栏 `reveal`→`hmi-right`（锚点 `<div class="reveal" style="transition-delay:150ms">` 与 `<div class="relative reveal flex justify-center">` 各语言唯一一致）；CSS 注入 `.reveal.visible` 块后；JS 注入 `revealObserver.observe` 行后（独立 hmiObserver，threshold 0.25，可重复触发）。
- 验证(dev 4321)：zh/es/ar/pt 的 /products 均 50A~150A×1、50A~200A×2、75A~150A=0、APF=0、hmi-left/right 类+translateX(±64px) CSS 在、HTTP 200。（注：zh 直接 /products，之前曾因 //products 双斜杠误判 404）
- 未提交 git。

## 新闻文章主图：8 张 webp 随机分配（全部 44 篇）
- 来源：`F:/下载/new1 (1..4).webp`、`F:/下载/new2 (1..4).webp`（8 张）。
- 拷贝入项目：`public/assets/images/news/news-img-1.webp` … `news-img-8.webp`（新建 news 子目录，不污染原 blog/ 资源）。
- 范围：zh(root `src/pages/news/`) + en/es/ar 各 11 篇 = 44 篇（pt 无 news）。不含 `index.astro`。
- 两种旧主图形态统一替换：
  - 渐变占位型（16 篇：carbon-neutral-factories/saudi-arabia-standards/sic-vs-igbt/solar-farm-dubai ×4 语）：`<div class="w-full h-64 md:h-96 bg-gradient-to-br from-.../10 to-.../5 rounded-xl ...">` 内含 SVG 图标。
  - 真实图片型（28 篇：blog-* + sic-ahf-launch + apf-industry-practitioner-2026 ×4 语）：`<div class="w-full max-h-[300px] rounded-xl overflow-hidden mb-10 bg-gray-50"><img class="...object-contain max-h-[300px]" .../></div>`。
  - 新块统一为：`<div class="w-full h-64 md:h-96 rounded-xl overflow-hidden mb-10 bg-gray-50"><img src="/assets/images/news/news-img-N.webp" alt="{文章标题}" class="w-full h-full object-cover" loading="eager" /></div>`（object-cover 横幅，左栏主图位不变）。
- 随机分配：**确定性哈希**（md5(文件绝对路径) % 8 + 1），每篇稳定分到一张、跨篇看似随机；构建可复现。分布：img1:1 img2:3 img3:2 img4:5 img5:1 img6:5 img7:4 img8:2（含之前已转 21 篇，本次新转 23 篇）。
- 坑①：en/news/solar-farm-dubai.astro 含损坏字节 `e2 86 3f`（原意应为 `→` 箭头，被写成 `?`），普通 UTF-8 读报 UnicodeDecodeError。用 `open(errors="surrogateescape")` 读写精确保留字节；结果该处被规范化为可读的 `0.85 to 0.99`（意外修复，语义正确，且文件现合法 UTF-8）。
- 坑②：早前运行已把 21 篇转成新格式，脚本对"已含 news-img-"的文件幂等跳过（不重复改、不报错）；旧 A/B 模式都不匹配时才跳过并报告，不中断。
- 验证(dev 4321)：首页/英文新闻页/图片均 200；en/solar-farm-dubai→news-img-4、zh/carbon-neutral-factories→news-img-1、es/blog-ahf-vs-line-reactors→news-img-6、ar/sic-vs-igbt→news-img-6 等已生效。
- 未提交 git。

## 新闻列表页（总页面）卡片左侧缩略图也换成 news-img 随机图
- 用户补充：news 总/列表页左侧的图（每篇文章卡片左侧缩略图）也要用这 8 张随机图。
- 范围：4 个列表页 `src/pages/news.astro`(zh) / `src/pages/en/news.astro`(en) / `src/pages/es/news/index.astro`(es) / `src/pages/ar/news/index.astro`(ar)，各 11 张卡片，共 44 张缩略图。
- 旧形态：`<div class="relative w-full md:w-[320px] lg:w-[380px] h-48 md:h-auto bg-gradient-to-br from-{color}-500/10 ... flex items-center justify-center shrink-0">` 内含 SVG 图标。
- 新块：同样外框类 + `<img src="/assets/images/news/news-img-N.webp" alt="{卡片h3标题}" class="w-full h-full object-cover" loading="lazy" />`（object-cover 填充，布局不变）。
- **一致性关键**：第一版用 `md5(详情文件路径)%8` 派生列表图，但详情页那次脚本的路径是 glob 返回的 `f:\下载\...`（小写 f、反斜杠），本次用 `F:/下载/...`（大写 F、正斜杠）→ 同篇文章列表/详情图不一致（如 zh solar-farm-dubai 列表1/详情5）。**改为直接读取对应详情页已分配的真实图号**（按 语言+slug 定位 `src/pages/{lang}/news/{slug}.astro`），保证列表卡片与详情页同图、跨语言各自一致。已全量校验 44 张卡片 0 mismatch。
- 坑：en 列表页卡片 `href={lp('/news/slug')}` 是 Astro 表达式（非字面量），正则需同时匹配 `href="..."` 与 `href=\{...\}`。
- 用 surrogateescape 读写（防损坏字节），幂等（已含 news-img 跳过）。
- 验证(dev 4321)：/news、/en/news、/es/news、/ar/news 均 200，各页 11 张 news-img，旧渐变=0。
- 未提交 git。

## news 列表页：图片容器统一大小 + 图片跨语言同步
- 用户两点要求：① 列表页卡片图片容器要一样大；② 图片修改所有语言页面同步（同一文章 4 语同图）。
- **容器统一**：列表卡片原 `h-48 md:h-auto`（桌面 h-auto + object-cover 使不同比例原图撑出不同高度，卡片高低不齐）→ 改为固定 `h-48 md:h-64`（4 列表页各 11 处）。详情页容器 `h-64 md:h-96` 未动（本就统一）。
- **跨语言同步**：原按"各语言详情文件路径哈希"分配，导致同 slug 在 zh/en/es/ar 图不同。改为**按 slug 决定图片**：收集全部 11 slug，按 md5(slug) 排序后用轮转 `(i%8)+1` 分配，保证 8 张图全用到（img1-3 各2、img4-8 各1）、确定可复现。同一文章在 4 语详情页+列表页全部同图。
- 校验：88 处（44 详情+44 列表）图号 0 mismatch；dev 4 语言 /news 均 200、11 图、11 统一容器；详情容器 intact。
- 未提交 git。

## SEO 策略讨论：active harmonic filter indonesia
- 用户观察：搜 "active harmonic filter indonesia"，前排全是印尼本地站(.co.id/.id)，且都是文字多、对应语言的独立产品页。结论：Google 奖励"搜索友好、有对应语言"的产品页。
- SERP 实测（2026-07-14）：前5 = BIM Indonesia / MSA Electrical / Intraco Indonesia / Oscorp Indonesia，均为印尼域名 + 印尼语/英文本地内容 + 独立 AHF 产品页 + 原理/参数长文。
- 我方现状：无独立 AHF 页（AHF 只是 2600 行 products.astro 的一个 section，title/H1/URL 均无 "active harmonic filter" 关键词）；结构化数据仅 Organization+WebSite；全站无 id 语种、无印尼市场内容（仅西语博客提一次）。
- 战略分层（与用户共识方向，未实施）：
  - Tier1 全球词 "active harmonic filter"：可攻，杠杆 = 独立英文 AHF 页 + 富文本 + Product/FAQ/Breadcrumb schema，完全自控。
  - Tier2 地域词 "...indonesia"：难攻，需印尼语(id)页 + 本地域名/外链；英文 Indonesia 专节仅能吃部分（说英语采购/跨国）。用户洞察"排名高的都有对应语言页面" → 语言匹配是关键信号。
- 决策待定：是否上 id 语种打 Tier2，还是先赢 Tier1。用户当前选择：纯讨论，暂不实施。
- "搜索友好"实施要点（将来建页必做）：关键词进 URL/H1/title/首段；关键文字用服务端 HTML 而非 canvas/JS（现有 products 页 hero 用 canvas、matrix 用 JS 渲染，AHF 新页要保证原理/参数/FAQ 是静态 HTML）；Product+FAQPage+BreadcrumbList 结构化数据；唯一 title/description；hreflang 正确；nav+博客内链；内容深度 800+ 词。

## 可抓取性实测（dist 构建产物，2026-07-14）
- 结论：**首页/index 是可抓的静态 HTML，"都是JS抓不到"的担心不成立**。
- dist/en/index.html：**0 canvas**，H1 真实文字在（"Facilitate harmonious coexistence of…" / "Leading OEM Provider of Low-Voltage…"，文字在 h1>span 内，裸 grep h1 取不到但内容存在），可见纯文字 7218 字符 → 完全服务端渲染，可抓。
- dist/en/products/index.html：206KB，可见纯文字 27992 字符，H1/H2/H3（Pain Points/Core Technology/Four Product Series/Success Stories 等）均静态 HTML，可抓。
- **唯一真正的抓取黑洞**：产品页选型矩阵 `<tbody id="matrix-tbody"><!-- JS fills this --></tbody>` 是空壳，全部 AHF 型号/规格由客户端 JS 填充 → Google 抓不到具体型号编码/参数。最该修的点。
- 2 canvas（hero-canvas/vp-canvas）均 pointer-events-none 纯背景动画，非内容，不影响 SEO。
- 修复优先级：① 选型矩阵改服务端渲染或加静态 HTML 表格 fallback（型号/规格可抓）；② 结构问题仍是"扁平 /products 大页 + URL 无关键词 + 无产品分类层级/面包屑"（对手为深层 Product Center→分类→子分类→单品 树状 IA）。

## 决定：采用方案2（CHITEK 产品 IA 提案），不实施
- 用户指令："依然不执行。我们产品很少，先给出2的方案。" → 只出信息架构提案，不动代码。
- 关键约束：**产品很少（约 4 个产品族：AHF / SVG / SiC / AVR）** → IA 必须浅（2 层），不能抄对手 4 层深树（会造薄页、稀释权重）。
- 提案要点（口头给出，待确认）：
  - URL 树：`/products`(hub) → `/products/active-harmonic-filter`、`/products/static-var-generator`、`/products/sic-ahf-svg`、`/products/automatic-voltage-regulator`（各语前缀一致，hreflang 完整）。
  - 每族页：slug 含精确词、H1=产品名、独立 title/description、BreadcrumbList + Product schema、规格表改服务端渲染（修 matrix JS 黑洞）、FAQ、Indonesia 小节（英文，不上 id）。
  - 现有 2600 行 mega `products.astro` 降级为 hub（4 族概览 + 链接），扛关键词任务交独立 AHF 页。
  - 语言：先 en+zh，再 es/ar/pt。
- 待用户确认 IA 后再起草 AHF 页内容大纲 / 开工。

## 三级小页方案细化（17:17，仍未实施，纯方案）
- 用户 5 条修订（基于方案2）：
  1. URL 树**去掉 AVR**；**设第三层（系列页）**。保留 3 family：active-harmonic-filter / static-var-generator / sic-ahf-svg。
  2. Description 规则：国家名只在"对应语言 / 对应谷歌地理定位"页出现；EN 全局页描述**不含国家**。参考站 coepowers.com/product/active-harmonic-filter/（结构：Category+Tags 长尾内链、编号章节、FAQ、项目内链、spec 表）。
  3. **先不处理** mega products.astro→hub 降级 & matrix JS 修复；先把三级系列小页方案做扎实。
  4. Indonesia 小节**暂不管**。
  5. 语言：**先敲定并改完英文方案，再同步 zh/es/ar/pt**。
- AHF 三级系列（products.astro 矩阵头确认）：Ultra-Slim(U)/Distributed(D)/Anti-Pollution(A)/SiC(C)/Standard(L) 共5 → /products/active-harmonic-filter/{ultra-slim|distributed|anti-pollution|sic|standard}/。
- SVG / sic-ahf-svg 系列名待确认（仅确认 AHF 5 系列）。
- 待确认：sic-ahf-svg 是独立 family 还是并入 AHF/SVG 的 SiC 系列（当前产品页 SiC 是 AHF 与 SVG 各自的 C-Series）。

## 三级树锁定（17:27）
- 用户确认：SVG 系列 = 与 AHF 完全相同（Standard L / SiC C / Distributed D / Anti-Pollution A / Ultra-Slim U 共5）；sic-ahf-svg = **独立 family**。
- **完整三级树锁定**：
  - `/products` (hub)
  - `/products/active-harmonic-filter/` + 5 系列页（standard/sic/distributed/anti-pollution/ultra-slim）
  - `/products/static-var-generator/` + 同 5 系列页
  - `/products/sic-ahf-svg/` + 系列页（系列名待确认，建议沿用同 5 模式）
- 已起草 AHF family + 5 系列页内容大纲（见回复），仍为纯方案、未实施。
- 规则重申：EN 优先敲定改完才同步 zh/es/ar/pt；description 国家名仅对应语言/geo 页出现；Indonesia 小节暂缓；mega 页降级 & matrix JS 修复暂缓。

## 结构改为二级（17:41）
- 用户："采用二级结构就行了" → **取消 L3 系列页**。最终结构 = `/products`(hub) + 3 family 页（active-harmonic-filter / static-var-generator / sic-ahf-svg），**无 AVR**。
- 5 个系列（Standard/SiC/Distributed/Anti-Pollution/Ultra-Slim）降级为 family 页内的 **H3 锚点小节**，不再单独占 URL（避免薄页、单产品一个强目标）。
- family 页承担全部内容（含 5 系列小节），目标字数提到 1500–2000。
- 仍为纯方案，未实施；等"开工"再建英文 3 个 family 页。

## AHF 英文 family 页已建（17:4x）
- 新建 src/pages/en/products/active-harmonic-filter.astro（二级结构 L2，已实装）。
- 内容：Hero + 面包屑 + Overview / How It Works(5步) / Key Benefits / Technical Specifications(**静态表**) / Product Series(5个H3: Standard/SiC/Distributed/Anti-Pollution/Ultra-Slim) / Applications / AHF vs Passive / FAQ(9条 `<details>` 可见) / Related(SVG+SiC链接) / 长尾Tags块 / CTA。
- 结构化数据：BreadcrumbList(2级) + Product + FAQPage，JSON-LD 与可见 FAQ 对应。
- 关键内容全静态 HTML（规格表/FAQ 不用 JS 隐藏），可抓取。
- dev(4321) 验证：HTTP 200；H1 / 规格表 / FAQPage / BreadcrumbList / Product / 可见FAQ / 长尾tag 均在静态 HTML 中。
- 未同步其它语言（EN 优先规则）；SVG / SiC family 页未建（相关链接暂 404，待建）；未提交 git。

## News 提升为 About 与 Contact 之间的顶级导航标签（src/components/Nav.astro）
- 用户要求：news 变成 about 和 contact 之间的新标签栏，并同步其它语言修改。

## en/news.astro 卡片按日期降序重排（17:5x）
- 原顺序乱：2026 年文章被插在 2025 年之后、2024 之前。按 news 规范（"日期设最近日期确保排列表顶部"）用脚本全局降序重排 16 张卡片。
- 顺序：2026-05-31 → 2024-10-05（SVG Factory Guide 居首，Carbon Neutral Factories 末位）。仅改 `src/pages/en/news.astro`，其他语言(zh/es/ar)未动（用户仅要求英文）。
- 脚本：C:\Users\lenovo\AppData\Local\Temp\sort_news.py；原文件备份 news_astro_backup.txt（同 temp）。
- dev(4321) 验证：HTTP 200，渲染页 16 张卡片日期降序正确。未提交 git。
- 改动（共享 Nav 组件，一次编辑即同步 5 语）：
  - 桌面端 + 移动端均从 About 下拉中**移除**冗余 News 子项。
  - 桌面端 + 移动端均在 About 与 Contact 之间**新增** News 顶级标签（独立 a 链接，带 hover 下划线/品牌橙高亮）。
  - 保留 `lang === 'zh'||'en'||'es'||'ar'` 守卫 → pt（无 news 页）仍隐藏 News。
- newsHref 由 Nav 现有逻辑推导（zh `/news`、en/es/ar `/{lang}/news`）。
- 验证(dev 4321 5 语 curl)：导航顶级顺序均正确
  - zh/en/es/ar：`/products /solutions /services /about /news /contact`（200）
  - pt：`/pt/products /pt/solutions /pt/services /pt/about /pt/contact`（200，无 /news，符合预期）
- 未提交 git。

## 最新三篇新闻日期改为均匀分布到 2026 年 5 月
- 用户要求：最新 3 篇文章日期改成均匀分布到 26 年五月。
- 目标 3 篇（列表页顶部、4 语同 slug）：svg-reactive-power-compensation-factory-guide / distributed-small-capacity-apf-guide / shenzhen-qidian-svg-analysis（原均 Jul 14, 2026）。
- 新日期（列表自上而下均匀分布，间隔 15 天，覆盖整月）：
  - 第1(svg-reactive) → 2026-05-31 / May 31, 2026 / 2026年5月31日 / 31 مايو 2026
  - 第2(distributed-apf) → 2026-05-16 / May 16, 2026 / 2026年5月16日 / 16 مايو 2026
  - 第3(shenzhen-qidian) → 2026-05-01 / May 1, 2026 / 2026年5月1日 / 1 مايو 2026
- 改动范围（共 16 文件）：
  - 4 列表页：卡片日期 span（zh/en/es 英文、ar 阿拉伯文）+ HTML 注释同步更新。
  - 12 详情页（3 slug × 4 语）：frontmatter `date` + `dateDisplay`，及 JSON-LD `datePublished`/`dateModified`（每文件 2026-07-14 共 3 处全替换为新 ISO）。
- 校验：旧串(Jul 14,2026 / 14 يوليو 2026 / 2026年7月14日 / 2026-07-14)全 0；每详情文件新 ISO 计数=3、dateDisplay 计数=1；4 列表页新显示日期顺序正确。
- 注意（潜在时序矛盾，尚未处理）：第4篇起为 Jun/Jul 2026（apf-industry-practitioner Jul13、blog-chitek-ahf-advantages Jul9、blog-active-harmonic Jun15…），改后顶部 3 篇为 5 月、其下却为 6/7 月，列表日期不再严格递减。已向用户说明，待确认是否一并回填/重排。
- 未提交 git。

## 第4~8篇回填到 2026 年 2–4 月（方案1，严格递减）
- 用户确认：选方案1（把第4篇起的 Jun/Jul 2026 文章回填到 5 月之前），且强调「文章一定是最新日期的排前面」。
- 回填 5 篇（4 语各 1 详情页 = 20 文件）+ 4 列表页对应卡片：
  - apf-industry-practitioner-2026：2026-07-13 → 2026-04-28（2026年4月28日 / Apr 28, 2026 / 28 أبريل 2026）
  - blog-chitek-ahf-advantages：2026-07-09 → 2026-04-12（2026年4月12日 / Apr 12, 2026 / 12 أبريل 2026）
  - blog-active-harmonic-filter-for-vfd-systems：2026-06-15 → 2026-03-27（2026年3月27日 / Mar 27, 2026 / 27 مارس 2026）
  - blog-ahf-vs-line-reactors：2026-06-08 → 2026-03-11（2026年3月11日 / Mar 11, 2026 / 11 مارس 2026）
  - blog-svg-power-electronics-daily-life：2026-06-01 → 2026-02-22（2026年2月22日 / Feb 22, 2026 / 22 فبراير 2026）
- 每详情页替换：frontmatter `date`(1) + JSON-LD `datePublished`/`dateModified`(2) 共 3 处旧 ISO → 新 ISO；`dateDisplay` 正则替换(1)。
- 4 列表页：中间 5 张卡片日期 span（zh/en/es 英文、ar 英文——ar 第4~8篇本就用英文月份，仅前3篇 May 卡用阿拉伯文）+ HTML 注释同步。
- ⚠️ 预存列表-href 不一致：列表页链接 `/news/blog-active-harmonic-for-vfd-systems` 漏了 `filter`，但真实详情文件 slug 为 `blog-active-harmonic-filter-for-vfd-systems`（含 filter）。本次未修（非本次引入），待用户确认是否统一（建议把 href 补 `filter` 与文件一致）。
- 最终 8 篇日期序列（新→旧，4 语一致）：May 31 → May 16 → May 1 → Apr 28 → Apr 12 → Mar 27 → Mar 11 → Feb 22，严格递减，满足「最新在前」。
- 校验：旧串(Jul 13/9、Jun 15/8/1 对应各语显示)全 0；每详情旧 ISO 计数=3、dateDisplay 计数=1；每列表旧英文日期计数=2；脚本全部断言通过、exit 0。
- 临时脚本 `tmp_redate_may2026.py` 运行后已删除。
- 未提交 git。

## 英文首页 Product Series 中间功能网格：自上而下错峰浮现（可反复触发）
- 用户要求：英语页面 Product Series 区域「中间部分」增加出现效果，从上到下出现，可反复触发。
- 范围：仅 `src/pages/en/index.astro`（用户明确说英语页面，未同步其它语言）。
- 背景：中间功能网格（6 张卡 2×3）原带惰性 `fade-up`（无 CSS 规则、未进 observer，等于静态显示）。
- 改动：
  - 容器去 `fade-up`（纯无操作类）。
  - 6 张卡各加 `ps-card` 类（replace_all 同字符串一次加完）。
  - observer 选择器加 `.ps-card`；回调新增分支：`psCards.indexOf(el) * 90ms` setTimeout 加 `.visible`（沿用 insight-card 错峰模式，离屏移除 `.visible` → 可反复触发）。
  - CSS：`.ps-card{opacity:0;transform:translateY(36px);transition:opacity .55s,transform .55s,background-color 200ms,color 200ms}` + `.ps-card.visible{opacity:1;transform:none}`（保留 hover 背景过渡 200ms）；`prefers-reduced-motion` 兜底加 `.ps-card`。
  - 错峰按 DOM 顺序（card0 左上→card5 右下）= 视觉自上而下；阈值 threshold 0.12 逐卡判定。
- 验证(Playwright 无头 1280×800，dev 4321 已运行，命中 `/en/`)：INIT_HIDDEN/ALL_VISIBLE/RESET/RETRIGGER 全 True；早期不透明度采样 [0.67,0.58,0.32,0.04,0,0] 确认自上而下级联；控制台 0 报错。RESULT PASS。
- 未提交 git。

## 英文首页 Product Series AHF 描述改写（en/index.astro）
- 用户要求把 "Active Power Filters" 改为 "Active Harmonic Filters"，并增加"是什么+应用场景"英文描述。先给草稿确认，后改。
- 改动：id=`info-ahf` 的 `<p>` 整段替换（用 Python 断言脚本，避免缩进静默失败）。新文案："Active Harmonic Filters (AHF) covering 208V to 800V, with efficiency up to 99%. An AHF is a real-time, IGBT-based or Sic-based active filter that detects harmonic currents drawn by non-linear loads and injects equal, opposite currents to cancel them at the source — restoring a clean sinusoidal waveform and correcting power factor. Used across industrial plants, commercial buildings, data centers, hospitals, EV charging and renewable sites where harmonic distortion, PF penalties or grid instability are a concern."
- 注：用户原文写 "Sic-based"（疑为 SiC-based 笔误），按用户确认文案逐字保留未改。
- 验证：源码新文案 1 处、旧 "Active Power Filters" 0 处；/en/ 200、served HTML 含新描述。
- 未提交 git。

## 英文首页 Product Series SVG 描述改写（en/index.astro）
- 同 AHF 流程：先给草稿确认后改。id=`info-svg` 的 `<p>` 整段替换（Python 断言脚本）。
- 新文案："CHITEK Static Var Generator (SVG) employs advanced IGBT or Sic switching at up to 25.6kHz to precisely regulate inverter voltage magnitude and phase. An SVG is a real-time reactive power compensator that injects or absorbs reactive current to dynamically correct power factor and stabilize voltage — eliminating penalties, sag and flicker. Used across industrial plants, commercial buildings, data centers, renewable farms, EV charging hubs and utility networks where reactive power, voltage fluctuation or load imbalance are a concern."
- 旧文案（已删）："CHITEK Static Var Generator employs advanced IGBT switching... Delivers dynamic reactive power compensation and three-phase load balancing with exceptional accuracy for demanding industrial applications."
- 注："Sic" 同样疑为 SiC 笔误，按用户确认文案逐字保留。
- 验证：源码新文案 1 处、旧串 0 处；/en/ 200、served HTML 含新 SVG 描述。
- 未提交 git。

## 将 AHF/SVG 新描述同步到 zh/es/pt/ar 首页
- 用户要求 "其它语言同步更改"：把英文首页新增的 AHF/SVG「是什么+应用场景」描述翻译同步到 zh/es/ar/pt。
- 方法：Python 脚本按 `id="info-ahf"`/`id="info-svg"` 定位各自 `<p>`，字符串索引截取旧 inner 替换新译文（首版用 re.escape 正则失败：re.escape 破坏引号/空格匹配，改纯字符串 index 法成功）。
- 翻译要点：4 语均用正确术语 **SiC**（英文源文案是 "Sic" 笔误，翻译里纠正为 SiC）；ar 原 SVG 文案里残留英文 "reactive power" 一并清理为阿拉伯语。
- 校验：8 个区块全部替换成功；4 语言首页 dev /、/es/、/pt/、/ar/ 均 200；served HTML 各含新译文、旧文案计数 0。
- 临时脚本与输出已删除；未提交 git。

## 将英文首页 hero/about 结构同步到 zh/es/pt/ar（完全对齐）
- 用户选 "完全对齐英文结构"。英文 hero=双文字面板轮播（面板0=Facilitate harmonious coexistence…；面板1=原AHF/SVG OEM文案，文字随2图+指示点切换）；about=去顶部标题区+新增底部统计数字行（2019/2700+/30+/20+）。
- 方法：Python 脚本（断言守卫）对 zh/es/pt/ar 逐个：① 用 `  <!-- HERO -->`/`  <!-- ABOUT -->` 锚点整体替换 hero `<section>` 为翻译版双面板轮播；② 删 about 顶部标题区；③ 在 about `</section>` 前插入翻译版统计行；④ 把英文 `<script>`/`<style>` 整段覆盖过去（最可靠，保证 stats 计数动画+hero-text 切换+`.hero-text`/`.stat-number` CSS 全到位）。
- 关键点/坑：
  - 从 en 源正则抽取 `<script>.*?</script>`/`<style>.*?</style>` 再 re.sub 覆盖各语言，避免手抄出错。各语言均有 `id="product-visual"`，脚本 product-tab 逻辑安全。
  - `.fade-up` 显隐由全局 `src/styles/global.css` 的 `.fade-up`/`.fade-up.visible` + 全局 observer 处理，覆盖本地 style 不会让内容隐形。
  - ar 的 about 顶部标题区**无** `<!-- 顶部标题区域 -->` 注释（其它三语有）→ 删除正则改为「注释可选 + 按 `relative z-10 text-center px-5 pt-6 pb-4 max-w-[900px]...` 该 div 类」匹配，四语通用。
  - 断言 `class="hero-dot` 会误匹配 `class="hero-dots`（容器）→ 改数 `data-slide=`(=2) 校验。
  - ar hero-text 两面板加 `text-right`（RTL）。
- 校验：4 语言源码 stat-number=4、hero-text=2、顶部标题 h2=0；dev /、/es/、/pt/、/ar/ 均 200 且 served HTML 含新 hero 面板0 文案与统计标签。
- 临时脚本与输出已删除；未提交 git。

## 将英文首页 Product Series 中间网格「自上而下」浮现效果同步到 zh/es/ar/pt
- 用户："同步其它语言"。把刚加的英文首页 Product Series 中间 6 卡「自上而下错峰浮现、可反复触发」效果（.ps-card）同步到其余 4 语首页。
- 范围：src/pages/index.astro(zh) / es/index.astro / ar/index.astro / pt/index.astro（与英文完全同源，DOM 相同）。
- 用带断言+幂等 Python 脚本一次性同步（每文件断言：容器去 fade-up=1、卡片加 ps-card=6、observer 选择器+.ps-card + psCards 收集 + else-if 分支=3、CSS 插入=1、reduced-motion 加 .ps-card=1；ps-card 总数=12）。首跑因断言计数笔误（误算 10，实为 12）在首个文件(zh)写后报错中断，仅 zh 落地；修正重跑：zh 跳过（已含 ps-card）、es/ar/pt 补齐。
- 验证(Playwright 无头 1280×800，dev 4321，逐一 /、/es/、/ar/、/pt/`)：4 语 INIT_HIDDEN/ALL_VISIBLE/RESET/RETRIGGER 全 True、控制台 0 报错。RESULT PASS。
- 临时脚本 `tmp_sync_pscard.py` 已删除；未提交 git。

## 补齐 EN-only 3 篇新闻到 zh/es/ar 并同步列表页（Request C）
- 背景：用户"其它语言文章应该和英语文章同步，同时遵守readme"。确认范围：全量补齐（3 EN-only × 3 语言 = 9 新文章）+ 核对 13 篇共享文章一致性。
- 3 篇 EN-only 文章 slug：active-vs-passive-harmonic-filters(2025-12-15)/active-pfc-dynamic-reactive-compensation(2025-11-20)/conformal-coating-ahf-reliability(2025-10-25)。
- **zh 已完成**（见早期记录中的 Request B），本次续做 es/ar + 一致性检查。
- **ES 3 篇**（src/pages/es/news/）：
  - 遵循 ES 惯例：import `../../../`、`lang="es"`、category "Technology"（英文标签）、author "CHITEK Technical Team"、byline "Equipo Técnico CHITEK" / "Publicado"、share "Compartir:"、sidebar "Más Artículos"/"Productos Relacionados"/"Casos de Clientes"、href `/es/news/...`、产品链接 `/products#...` 无前、案例 `/es/news/solar-farm-dubai`、"Leer Más" + `ml-1` 箭头、返回 "Volver a Noticias"。
  - ES 列表页 `src/pages/es/news/index.astro` 顶部插入 3 卡片（img news-img-1/-4/-5，category "Tecnología"、日期 "Dec 15/Nov 20/Oct 25, 2025"）。
- **AR 3 篇**（src/pages/ar/news/）：
  - 遵循 AR 惯例：import `../../../`、`lang="ar"`、category "التكنولوجيا"（阿拉伯文）、author "FNI CHITEK الفني"、byline "FNI CHITEK الفني" / "نُشر"、share "مشاركة:"、sidebar "مقالات أخرى"/"منتجات ذات صلة"/"حالات العملاء"、href `/ar/news/...`、产品 `/products#...` 无前、案例 `/ar/news/solar-farm-dubai`、"اقرأ المزيد" + `mr-1` 箭头（RTL）、返回 "العودة إلى الأخبار"。
  - 阿拉伯文日期：15 ديسمبر 2025 / 20 نوفمبر 2025 / 25 أكتوبر 2025。
  - AR 列表页 `src/pages/ar/news/index.astro` 顶部插入 3 卡片（img news-img-1/-4/-5、category "تكنولوجيا"、阿拉伯文日期、"اقرأ المزيد" + `mr-1`）。
- **一致性核对**（Task #9）：
  - 各语言 16 篇文章（13 共享 + 3 新）= 64 文件全部存在 ✓
  - 13 个 slug 均跨 zh/en/es/ar：13×4 = 52 文件全部存在 ✓
  - pt/news/ 不存在 ✓（符合 pt 无新闻规则）
  - 3 新 slug 跨 zh/en/es/ar：12 文件全部存在 ✓
  - 3 语列表页各含 3 新卡片 ✓
  - 抽样结构检查：Sidebar(2)/JSON-LD(1)/lang(2)/BaseLayout(1) 全部通过 ✓
- 未提交 git。
- 待用户本地 `npm run build` 验证（本环境 dist 删除守卫阻止 build）。

## 将英文首页 Solutions 区域改动同步到 zh/es/ar/pt
- 用户要求：英语首页 Solutions 区域的改动同步到其它语言。
- 对比 en/index.astro 和 zh/es/ar/pt 的 Solutions 区域结构差异：英文版已简化为无 SVG 图标、`justify-end` 布局、h3 加橙色左边框 (`border-l-[3px] border-brand-orange pl-4`)、grid wrapper 去除淡入动画类（已由 insight-card 逐卡交错代替）、修复 `href={lp(...)}"` 多出的引号。
- 🔧 第一版脚本：用 `opacity-0 translate-y-6 transition-all duration-[600ms] ease fade-up"` 作 grid wrapper 锚点→`replace()` 错误地匹配了 3 处（About 区 + Product Series 标题 + Solutions grid），导致 About 和 PS 标题动画类丢失。用修复脚本以 `shadow-2xl ">` 和 `text-center mb-[clamp(16px,3vw,48px)] ">` 精确加回（assert count=1），维持 en 一致。
- ✅ 第二版最终同步正确：每文件 `border-l-[3px]`=3、`w-14 h-14 mb-8`=0、`min-h-[2.6em]`=0、`commercial')" class`=0、`max-w-[1320px].*opacity`=0、`opacity-0 translate-y-6`=2（仅 About 和 PS 标题）。Playwright 4 语实测：SOLUTIONS exists、CARDS=3、SVG=0、BORDER=3、VISIBLE=3、ERRORS=0 → PASS。
- 临时脚本已删除；未提交 git。
