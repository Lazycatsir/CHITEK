# 2026-09-14 工作日志

- **GA4 接入 BaseLayout.astro**：用户给了 GA4 测量 ID `G-HVJS1DXF00`。在共享布局 `<head>` 加 `is:inline` 的 gtag 双脚本，用 `{lang !== 'zh' && (...)}` 条件渲染——**中文页(`/`)不加载 GA4**（Google 在大陆被墙 + PIPL），en/es/pt/ar 四语正常加载。dev 验证：zh gtag=False，四语 gtag=True & GA_ID=True 全 OK。
- **GSC 走「网域属性」(Domain)**：用户选网域属性验证，只需在 DNS 加一条 TXT 记录（`google-site-verification=...`），**代码层无需改**（不写 HTML meta 标签）。验证后在 GSC 提交 `https://chitek-inno.com/sitemap-index.xml`（@astrojs/sitemap 已自动生成）。
- 环境坑：本机 Git Bash 的 grep/head/netstat 缺失（shim 报错），PowerShell 内联脚本被"编码命令"安全策略拦截；改用托管 Python（urllib + subprocess）完成端口清理/重启 dev/验证，最稳。dev 后台进程（run_in_background）比 Python Popen 常驻更可靠（Popen 子进程在脚本退出后被回收→预览代理 502）。
- **百度统计也接好了**：用户给了 Token `3b9dceb779c78363e60cb3aba11959bd`，在 BaseLayout.astro 加 `lang === 'zh'` 守卫的 `hm.js` 块——**仅中文页(`/`)加载百度统计**，四语仍走 GA4。dev 验证：zh baidu=True/ga4=False，en/es/pt/ar baidu=False/ga4=True，全部 OK。中文站需营业执照实名（百度统计+百度搜索资源平台均要），用户已知悉。
- **GSC 网域属性 DNS 验证完成**（用户在 GSC 后台加 TXT 记录）。sitemap 已实查线上存在：`https://chitek-inno.com/sitemap-index.xml` → sitemap-0.xml 含 155 个 URL（五语全）。提交步骤：GSC 网域属性 → 站点地图 → 粘贴 `sitemap-index.xml` → 提交。代码层无需改。注意 BaseLayout 的 GA4+百度统计改动尚未 push 上线。

## 独立站运营计划 v1.0（docs/，同日追加）

- 需求：制定 CHITEK 官网（5 语独立站）完整运营计划。
- 产物：
  - `F:/下载/CHITEK/docs/独立站运营计划.md`（可编辑主文档，~21KB）
  - `F:/下载/CHITEK/docs/独立站运营计划.html`（极简深色阅读版，~24KB）
  - ASCII 副本供 file:// 打开：`C:/Users/lenovo/WorkBuddy/00_中转站/CHITEK-website-operations-plan.{html,md}`
- 结构：现状盘点 → KPI → 关键词/内容策略（4 支柱集群）→ 站点改造 P0–P4 → CRO → 渠道（SEO 60/LinkedIn 15/付费 15/邮件线下 10）→ GA4 事件表 → 90 天冲刺表 → 预算 → 风险 → 30 选题 + 素材表。

### 本次盘点出的站点事实（后续可直接引用）
- 8 个产品系列（AHF/SVG × Standard/SiC/Distributed/Anti-Pollution），但**只有 1 个产品详情页** `src/pages/en/products/active-harmonic-filter.astro`。
- 案例数据（20+ 条，含治理前后指标）硬编码在 `src/pages/*/solutions.astro` 的 `cases` 数组，`link:'#'`，**不可索引** → 计划列为 P0 改造。
- 转化现状：联系页 Netlify Forms（带 honeypot）+ Sidebar 悬浮电话/邮件；**无任何事件埋点**（GA4/百度都只有 PV 层）。
- 联系方式：页脚 0755-23596807（全语言）；zh/contact 用 +86 19020586646 / yue.peng@chitek-inno.com；en/es/pt/ar 用 +86 18818994346 / leo.li@（Sidebar 按 lang==='zh' 分流邮箱）。
- 新闻：zh≈28 / en≈26 / es≈20 / ar≈20，pt 无新闻；4 个列表页硬编码 → P4 改 Content Collections。
- 缺：资源下载中心、FAQ 页、对比页、选型计算器。

### 环境备注
- 本轮 **Bash 工具整体失效**（`dirname/cd/ls` command not found，PortableGit shim 异常）→ 用 Glob/Grep/Read 完成盘点，文件复制用 PowerShell 工具。
- **Glob 对含中文的绝对路径失效**（查 `.workbuddy\memory\*.md`、中转站中文目录都返回空）→ 需用 Read 或 Grep 兜底。

