# 2026-07-24 工作日志

## 设计 CHITEK 项目交接文档包
- 按用户要求将 5 份 markdown 合为单文件 HTML：`docs/站点管理手册.html`
- 内容覆盖：项目概览、环境准备（OpenCode/Node/Git/GitHub/Netlify）、7 项日常任务（含完整操作提示）、详细工具清单（含验证清单）、故障排查 5 种场景
- 第 3 节（日常任务）和第 4 节（工具清单）重点展开：每个任务含准备步骤、精确提示语、幕后流程、验证方法、注意事项
- ��更新 CLAUDE.md：新增"交接后工作流程"和"常见构建失败原因"节（frontmatter 逗号/CRLF/BOM）
- 旧 markdown 文件夹因沙箱限制未删除，留在 `docs/交接文档/` 下。用户知悉。
- 用户要求简化权限模型：接收方不需要 GitHub 账号、不需要 Netlify 登录。改为只用 SSH Deploy Key（接收方生成密钥对 → 交接人把公钥加到 GitHub 仓库的 Deploy keys，勾 Write access）。
- 重写 `docs/站点管理手册.html`：去掉"注册 GitHub""Netlify 登录""Token 方式"全部相关步骤，4 节检查清单同步简化（只剩软件安装 5 项 + 权限配置 3 项 + 最终验证 4 项），故障排查节去掉所有 Netlify 登录步骤。

## services 页 Tech Hotline 电话更改
- 用户要求 tech hotline 改为 0755-23596807，所有语言。
- 改 5 个 `src/pages/{,en,es,pt,ar}/services.astro` 的 Tech Hotline 页脚框第二行（原 400-XXX-XXXX 占位符），保留各语言工作时间后缀。
- 未动 FAQ 里的 "Hotline: +86-755-XXXX-XXXX | WhatsApp" 行（与 WhatsApp 配对、格式不同，需用户确认是否也改）。

## Footer 订阅框下加联系方式
- 用户要求在页脚「订阅我们…」文案下加联系方式 0755-23596807，所有语言。
- 改共享组件 `src/components/Footer.astro`：新增 `contactPhone` 翻译键（en Contact / pt Contato / es Contacto / ar اتصل / zh 联系电话），并在 subscribeText 下方加一行 `<p>` 渲染。
- 该组件由 BaseLayout 全局引入 + 5 个 services 页直接引入，单文件改动覆盖全站所有语言页面。dev 验证 5 语首页 + services 页均正确渲染。

## 批量新增 5 篇 news 文章（四语）
- 5 篇主题：chitek-shanghai-power-expo-2026（行业动态）、lithium-battery-plant-power-quality（技术）、gb14549-harmonic-standard-compliance（技术）、rail-transit-traction-power-quality（技术）、sic-ahf-ce-ul-certification（产品更新）。
- 每篇生成 zh/en/es/ar 共 4 个 .astro 文章文件 → 20 个文件；并向 4 个语言列表页（news.astro / en/news.astro / es/news/index.astro / ar/news/index.astro）的 `<div class="space-y-6">` 顶部各插入 5 张新卡片（共 30 张/页）。
- 为避免 Edit/Write 对大段多语言内容的静默截断，改用 **Python 生成脚本直接写文件**：`F:/下载/CCACHE/gen_a.py`（文章1-3，写 CCACHE_cards_a.json）+ `F:/下载/CCACHE/gen_b.py`（文章4-5 + 读 cards_a 合并后插列表）。模板复用现有文章 frontmatter/JSON-LD/Sidebar 结构，en 用 `{lp('/news/..')}`（lp 在 frontmatter 定义），zh/es/ar 用绝对路径 `/news/..`、`/es/news/..`、`/ar/news/..`（lp 在 body 定义 = identity）。
- 图片用 public/assets/images/news/news-img-1..5.webp（共 8 张可用）。
- 验证：dev 服务 4 语列表各 30 卡片、20 篇文章页全 HTTP 200、无 `__PLACEHOLDER__` 泄漏、href 格式正确。
- **可复用技能候选**：多语言新闻批量生成（模板 + 生成脚本）属重复工作流，建议沉淀为 skill 或 MEMORY 笔记，下次直接套用。
