# 2026-09-17 工作日志

## en/products.astro 重构前的参照与流程决策

- 用户想**重构英文 products 总页**，担心 AI 读不到原代码 → 讨论「先复制还是靠 git」。
- 关键事实：`src/pages/en/products.astro` 现 **2680 行**，超过 Read 单文件 2000 行上限（需分段读）。
- 决策：① **先复制快照**作 AI 可读参照 + 即时回滚；② git 因项目惯例「不主动提交」无干净锚点，故**不靠 git**，除非用户明确要 commit checkpoint。
- 快照落地：`docs/_ref/en-products-orig.astro`（放 docs/ 而非 src/pages，原因：src/pages 下 `_` 前缀虽不被路由但仍有编译开销/风险；docs/ 完全不被 Astro 编译、不部署，纯参照文件）。
- 更大的 AI 可读性问题：2680 行单体页。真正的友好化应在重构时**拆成小组件/ProductLayout + 数据驱动**（与 2026-09-16 诊断一致），而非仅靠复制。
- dev 服务器隔夜停（端口无监听，urllib 502 是沙箱代理回包）；已清 .vite/.astro 缓存 + 托管 node 重启，4321 返回 200。

## en/products hero 高度改为满屏 1/3

- `src/pages/en/products.astro:25` `#hero` 的 `min-h-[70vh]` → `min-h-[33vh]`（用户要求 hero 只占满屏约 1/3）。`mt-[72px]`（顶栏）保留，hero-canvas 用 offsetHeight 自适应不受影响。
- 页面其余 `min-h-[70vh]` 是 case study 卡片模块，与 hero 无关，未动。

## 导航 Products → AHF 悬停飞出三组小卡片（图+文）

- `src/components/Nav.astro`：把 Products 下拉里的 AHF 那一项包成 `relative group/ahf`，悬停/键盘聚焦时右侧飞出 `lg:grid grid-cols-3` 三卡（上图下文，图用 index-product-{ahf,svg,avr}.webp，占位文字）。纯 CSS `group-hover/ahf` + `group-focus-within/ahf` 显隐，内容**始终在服务端 HTML**（Astro SSG），非 JS 注入。
- 验证：页面 HTML 含 Standard/SiC/Distributed AHF 三卡 + 占位文字；global.css 已编译出 `group-hover/ahf:visible/opacity-100`（@media hover:hover）与 focus-within 规则。
- 该 Nav 是**全语言共用组件** → 飞出在 zh/en/es/ar/pt 都出现；卡片文案现为英文占位，投产前需按语言翻译（或 zh 站不该显示英文卡标题）。
- 扩展：SVG(`#svg`)、SiC(`#sic`) 两项也加了同款飞出（命名组 `group/svg`、`group/sic`，各 3 张占位卡，内容用户后补）。验证：页面 HTML 含 Standard/SiC/Distributed SVG 与 SiC AHF/SVG/Cabinet 六卡；global.css 已编译 `group-hover/svg`、`group-hover/sic` 的 opacity/visible 规则。
- SEO：① 服务端渲染+真实内部链接（`/products#ahf`）→ 正向（内链+主题相关），无 cloaking；② 移动端 `hidden`（display:none）不显示卡片，但 AHF 链接在移动 accordion 里仍在 → 移动索引不受影响；③ 占位文字为中性，无关键词堆砌风险。投产建议：卡片改链到真实产品详情页、文案翻译、替换占位。

## en/products hero 副标题文案更新
- `Advanced power quality solutions...` → `Advanced power quality and power factor solutions for industrial and commercial applications.`（src/pages/en/products.astro:32）

## Contact 表单「Send Message」有效性核查（重要发现）
- 结论：**线上不生效**。实测 POST 到 `en.chitek-inno.com/contact/`、`chitek-inno.com/contact/`、`/` 均返回**站点自带 404 页**（`<title>Page Not Found – CHITEK INNOPOWER</title>`），非 Netlify Forms 拦截 → Netlify Forms 未注册/未接收提交。
- 结构层面没问题：live HTML 表单含 `data-netlify="true"` + 隐藏 `form-name=contact` + `netlify-honeypot=bot-field`（honeypot input 也在）。
- 但 3 个钩子**只存在于工作区、从未 commit**（`git log -S data-lead-form` 为空；`git show HEAD:src/pages/en/contact.astro` 无）。Netlify 从 git 部署 → 线上缺 `data-lead-form`/`data-utm-fields`/`data-form-success` → analytics.js 不接管表单 → 无 AJAX、无 `generate_lead`、无 UTM 采集、无站内成功提示 → 用户点 Send 走**原生 POST → 404 页**，消息丢失。
- `netlify/functions/contact-notify.js`：`POST /api/contact-notify` 实测返回 `200 {"success":true}`，但**仅 console.log，不发邮件/不存储，且表单未接它** → 死代码。
- 站点确认在 Netlify（响应头 `server: Netlify` + `X-Nf-Request-Id`）；本地 `dist/en/contact/index.html`（09-16）同样无三钩子（susp：dist 由 HEAD 而非工作区构建）。
- 修复方向：① Netlify 后台 Forms 确认是否存在「contact」并重部署；② commit 工作区三钩子 → 恢复增强层；③ Forms → 通知邮件设置。**未提交前勿宣称转化追踪已上线。**
- 校验手法：POST 提交看是否返回站点 404 页（=未注册）；响应头看 `server: Netlify`。

## Contact 表单：免费方案结论
- 用户要"免费方案"。核实：**Netlify Forms 本身免费**（Starter 100 次/月，含邮件通知+Akismet/honeypot 过滤；Netlify 新计费下低量近似免费）→ 免费首选就是把它修通，无需第三方。
- 实测线上 POST → 站点自带 404 → 表单未被 Netlify 登记。**最可能原因：Netlify 后台 "Form detection" 未开启**（Site configuration → Forms → Enable form detection）或该次部署未触发检测 → 需开启开关 + `Clear cache and deploy` 重新部署。
- 免费备选（若 Netlify Forms 仍识别不了）：静态站第三方表单后端，都免费额度内——**Static Forms（免费 500/月）**、**Web3Forms**、**FormSubmit.co（免注册）**；改 `action`/加 access key 即可，0 服务器 0 成本。
- 交付链路与追踪是两件事：免费修好投递后，仍需 commit 三钩子（data-lead-form 等）才能恢复 GA4/百度转化事件。
- 用户后台已配通知："Email 2511082534@qq.com on new submission from any form"（截图确认）。
- Netlify 通知邮件格式（官方 docs 核实）：发件人 `formresponses@netlify.com`；主题默认形如 "New form submission from contact"（可自定义，支持 %{formName}/%{siteName}/%{submissionId}；HTML hidden name="subject" 会覆盖 UI）；正文列出所有字段值；**因表单含 name="email" 字段 → Reply-To = 客户邮箱**，可直接回复客户。
- 复测（16:44）：用户在后台**开启了 Form detection 但未重新部署** → POST 仍 404（en 子域 + 主域都试）。确认根因=Form detection 之前是关的；开关须**配合 redeploy** 才生效。下一步=Deploys → Clear cache and deploy → 再测。
- 用户明确反馈：`data-lead-form` 只是"绑定 analytics 的标记"，**与询盘投递无关**（对，投递只靠 data-netlify + form-name）。勿再把它和"能否收到询盘"混为一谈。
- 官方 KB 明确修复路径："Confirm form detection is enabled in the UI, then redeploy. It only takes effect on the next deploy." + "Astro form never registered → 表单须在 prerender 页（本站 SSG，OK）"。→ 印证"开 Form detection + 重新部署"。

## SEO 收录配置审计（Bing/Google/Baidu）
- 审计 `BaseLayout.astro` head / `astro.config.mjs`(`@astrojs/sitemap`, site=chitek-inno.com) / `netlify.toml`(Bing 8 条 force 转发) / `public/robots.txt`+`BingSiteAuth.xml` / `src/i18n/index.ts`(hreflang 用子域 URL)。
- 已具备（三引擎通用）：`<html lang>`+`dir`、title/description、canonical、hreflang(zh/en/es/ar/pt+x-default)、OG/Twitter、JSON-LD(Organization+WebSite)、sitemap-index.xml（含子域 URL）、SSG 静态 HTML。
- Bing：验证完整（msvalidate.01 全语言 + BingSiteAuth.xml + 8 子域转发）。
- Google：hreflang/canonical/sitemap 齐；GSC 靠 DNS TXT 验证（代码层无 meta，需确认 TXT 记录已加）。
- Baidu：**不完整**——仅有 Baidu Tongji 统计(`_hmt`, zh)，**无 baidu-site-verification 验证**（无 meta、无 baidu_verify 文件），故无法在百度搜索资源平台认领/提交 sitemap → 收录未通。
- 🐞 修：`public/robots.txt` 原 `Sitemap: .../sitemap.xml` 但实际生成文件是 `sitemap-index.xml`（已查 dist 确认）→ 改为 `sitemap-index.xml`（三引擎 robots.txt 自动发现不再 404）。
- 代码层无法做的手动步骤：GSC/Bing/百度 各控制台提交 sitemap；确认 GSC DNS TXT；百度验证 code 需用户从百度搜索资源平台取得后我再加 meta。

## 底部栏 Footer 产品列标签调整
- 需求：底部栏①`Active Harmonic Filter`→`Active Harmonic Filter AHF`；②在其下新增`Static Var Generator SVG`；③`Advanced Static Var Generator`→`Advanced Static Var Generator ASVG`。
- 改 `src/components/Footer.astro`：翻译字典 `activeHarmonicFilter.en` 追加 `AHF`、`advancedSvg.en` 追加 `ASVG`；新增 `standardSvg` 键（5 语，SVG 缩写通用，各语 base + ' SVG'）；Products 列 `<ul>` 在 AHF `<li>` 后插入 `#svg` 的 standardSvg `<li>`。顺序：AHF→Static Var Generator SVG→Advanced SVG→SiC→High-Protection。
- 仅改 `en` 值（用户看英文站），其余语言原值不动；导航 Nav.astro 的 `activeHarmonicFilter` 是独立字典，不受影响。用 Python 脚本 + assert 落盘（避并行 Edit 静默空操作）。dev HMR 自动生效。

## 导航飞出卡片与左侧菜单高度对齐
- 需求：「飞出卡片和左边高度对齐比较美观」。
- 改 `src/components/Nav.astro`：`Products` 下拉面板加 `min-h-[210px] flex flex-col justify-center`（稳定高度 + 三行垂直居中）；去掉 AHF/SVG/SiC 三项 wrapper 的 `relative`（让飞出 `absolute` 的定位祖先回到面板本身）；三张飞出面板 `top-0` → `top-0 bottom-0`（拉伸至面板满高，边框对齐）。
- 验证：dev `http://localhost:4321/en/products` 返回 200；grep 确认三飞出均 `top-0 bottom-0`、无 `relative group/` 残留。Playwright CDP 实测不可用（127.0.0.1:9222 回 502 无浏览器），改纯标准 Tailwind 工具类（min-h-[]/flex/flex-col/justify-center/top-0/bottom-0 均 v4 原生支持），CSS 逻辑成立。
- ⚠️ 复盘：同一文件**并行发多条 Edit** 会静默空操作（本次仅 SiC 飞出生效，AHF/SVG 飞出 + 三处 wrapper 的 relative 移除全部没落盘，工具却报 success）。改用 Python 脚本（逐条 `assert count==1` 后 replace）一次性落盘并自检通过。规则：**批量改同一文件用脚本 + assert，勿并行 Edit**。

## 英文新闻列表页结构：单页全列 vs 分页（用户问 SEO 哪种好）

- **结论**：30 篇规模 → **单页全列**，不上分页。大白话理由：① 搜索引擎是"看打印稿的检查员"，不会点页面上的按钮，按钮后加载的第 9–30 篇等于不存在；② 真分页（每页独立网址）在 ≤40–50 篇不划算，多一跳多一次抓取延迟；③ 真问题不是列表页长短，而是**每篇文章只有 1 条入链**（见 09-16 内链审计），分页只会更差。
- **约定阈值**：英文文章数 > 60 篇 或 列表页 gzip > 100 KB（当前 gzip 35.5 KB / raw 313 KB）→ 再上 `/en/news/page/2` **真链接**（禁止纯 JS load-more；每页 self-canonical，title 带 Page 2）。

### 改了 `src/pages/en/news.astro`（脚本 `_patch_en_news.py` 落盘）
- 30 张卡片各加 `data-cat`（tech 22 / case 3 / product 2 / industry 2 / sustain 1），由卡片自带分类 chip 文本自动推导，脚本 assert tagged==30。
- **删掉假分页**：原 721–738 行是 1…8 数字分页器，6 个 `href="#"` 全死链（骗用户有 8 页）。
- 换成**话题筛选条**（All / Technology / Case Study / Product Update / Industry News / Sustainability）+ `Showing all 30 articles` 计数行。
- SEO 关键点：**筛选只改 `style.display`，30 条链接始终在 HTML 里 → 爬虫不受影响**；脚本整体 try/catch，出错则全卡片可见（fail open）。
- 样式走 scoped `<style>` + `.nf-btn`（避开 Tailwind 扫不到 JS 里拼的类），颜色用 `var(--color-brand-orange,#ff6b1a)` 等 CSS 变量。

### 验证
- dev `http://127.0.0.1:4321/en/news` HTTP 200：`data-cat` 30、`nf-btn` 9、计数文案在、`href="#"` 6→1（余下 1 个在全局 Nav/Sidebar）。
- 标签配平：a 30/30、section 2/2、div 96/96、script 1/1、style 1/1。gzip 35.5 KB，卡片图已全部 `loading="lazy"`。

### 遗留（下次）
- zh/es/ar 列表页仍是旧结构（zh 同样有 6 个假分页 `href="#"`）；本轮用户只要英语。
- en 列表页 `blog-hospital-ahf-solutions` 卡片的标题/描述错贴成 selection guide 的内容（脏数据）。
- 治本是给每篇英文文章加底部「Related reading」3 条同类内链 —— 未做，下一步。

## 追加：用户改主意 → 真分页 10 条/页（上一版的话题筛选条已撤）

- 用户要求：去掉话题筛选条，改成**一页 10 张卡片**。已按「真分页（每页真实网址）」实现，不用 JS 按钮切换（避免爬虫看不到第 11 篇以后）。
- **新结构**：`/en/news/`（1–10）· `/en/news/page/2/`（11–20）· `/en/news/page/3/`（21–30）；底部 pager 是真 `<a href>`（Prev / 1 2 3 / Next，当前页为 span），带 `aria-label="News pagination"` + rel prev/next。
- **卡片数据化**：30 张硬编码卡片抽成 `src/data/enNewsCards.js`（脚本 `_extract_en_news.py` 生成，源文件快照 `docs/_ref/en-news-orig-20260917.astro`）。字段：cat/href/img/alt/chipClass/category/date/title/desc；额外导出 `NEWS_PER_PAGE`、`NEWS_TOTAL_PAGES`。原列表页 774 行 → 67 行。
- 新增组件：`src/components/news/NewsCardEn.astro`（分类色 chip 用组件内静态 Map，保证 Tailwind 扫得到）、`src/components/news/NewsPagerEn.astro`（`url` 以函数 prop 传入）。
- **`BaseLayout.astro` 加 `alternates?: boolean`（默认 true）**：分页页 2/3 传 `alternates={false}`，否则 hreflang 会输出 zh/es/ar/pt 的 `/news/page/2/`（这些语言没有分页 → 404）。已验证：page1 hreflang=6，page2/3 hreflang=0。canonical 仍自指（`https://en.chitek-inno.com/news/page/2/`）。
- ⚠️ **Astro 大坑（务必记住）**：`export function getStaticPaths()` 会在**该文件顶层 const 求值之前**被调用 → 引用本文件里的 `const total` 直接 `ReferenceError: total is not defined`（dev 日志可见，浏览器只有标题）。**getStaticPaths 里只能用 import 进来的值或自己内部算**。已把 PER_PAGE/总页数放进数据文件导出解决。
- 验证（dev 4322）：三页均 200；每页 10 张卡片，跨页去重后正好 30 条、无重复无遗漏；pager 链接正确（page2 有回 `/en/news/` 的链接，page1 无 prev、page3 无 next）；计数文案 `Showing 1–10 of 30 articles · Page 1 of 3` 等三页正确。
- 清理：一次性脚本 `_news_scan.py` / `_patch_en_news.py` / `_news_en_new.astro` / `_err500.html` / `_ports.txt` 已移到 `F:\trash\chitek-news-tmp-20260917-134601`（保留 `_extract_en_news.py` 供下次重建数据）。
- 待办：sitemap 需重新 build 才会含 page/2、page/3（dev 不生成 sitemap）；build 仍受 dist 删除守卫限制，需用户本地跑。

## en/news hero 高度对齐 en/products（min-h-[33vh]）

- `src/pages/en/news.astro` 与 `src/pages/en/news/page/[page].astro` 两处 hero 同步改：
  `pt-[72px] … relative overflow-hidden` + 内层 `py-20 md:py-28`
  → `relative w-full overflow-hidden min-h-[33vh] mt-[72px] flex items-center bg-bg-dark text-white` + 内层加 `w-full`、去掉 `py-20 md:py-28`（内容垂直居中）。
- 与 products 保持一致：products 用 `mt-[72px]`（不是 pt），故 news 也改 mt。
- 验证：三页渲染 HTML 均含 `min-h-[33vh] mt-[72px] flex items-center`；残留的 `py-20 md:py-28` 是 `#articles` 区块，不是 hero。
## 英文新闻页去掉 "Latest Articles" 标题块

- `src/pages/en/news.astro` 删掉 `<h2>Latest Articles</h2>` + 副标题 `Explore our latest news, product updates, and company announcements.`；计数行的 `-mt-8` 一并去掉（原为抵消被删段落的间距，留着会把计数行顶进 hero）。
- `src/pages/en/news/page/[page].astro` 同步删掉 `<h2>Article Archive</h2>` + 副标题（保持三页外观一致；若要恢复单独说）。
- 顺手修标题层级：删掉 h2 后会变成 h1→h3 断档，故 `NewsCardEn.astro` 卡片标题 `h3` → `h2`（样式类不变）。验证：/en/news/ 现为 1×h1 + 10×h2 + 0×h3。

## 分页 URL 扁平化：/en/news/page/2/ → /en/news/page_2/

- 用户要求去掉中间 page 层级。做法：**文件名局部动态段** `src/pages/en/news/page_[page].astro`（**Astro 支持** `page_[page]` 这种"一段里带动态参数"的写法，实测 dev 200）。
- 分页器 URL 改 `lp('/news/page_' + n + '/')`；旧目录 `src/pages/en/news/page/[page].astro` 已移到 `F:\trash\chitek-news-oldpage-20260917-135241`。
- 验证：三页 200（10 卡/页、30 条唯一无重复）、canonical 自指 `…/news/page_2/`、pager 链接正确、旧 `/en/news/page/2/` 已 404。
- **无需重定向**：旧 URL 是今天才建的、从未 build/上线，线上不存在，故 Netlify 不用加规则；子域通配 `en/*` → `/en/:splat` 天然覆盖 `/news/page_2/`。

## 英文新闻页：去掉计数行 + 收紧 hero 与列表的间距

- 删掉 `Showing 1–10 of 30 articles · Page 1 of 3` 那行（`news.astro` 与 `page_[page].astro` 两处），同时清掉随之无用的 `from`/`to` const（避免悬空变量）。
- 列表区 `#articles` 的 `py-20 md:py-28` → `pt-10 md:pt-14 pb-16 md:pb-20`，hero 与卡片之间的空白明显收窄。
- 验证（dev 4323）：三页均无 Showing 文案、`#articles` 带 `pt-10 md:pt-14 pb-16 md:pb-20`、每页 10 张卡片。
- 备注：dev 服务已从 4322（进程退出）迁到 4323，预览 `http://localhost:4323/en/news/`。

- ⚠️ 不一致待确认：**中文站 products 仍是 `min-h-[70vh]`**（英文站今天才被改成 33vh），中文 news.astro 仍是旧的 `pt-[72px]` hero。用户本轮只要英文，若中文站也要统一需再确认 products 到底按 70vh 还是 33vh。
## en/products.astro 网格/列表卡片内容约定（13:34）
- 用户要求：网格视图(grid)下每张卡只显示主标题(.h4)+ 简要描述(.cbp-vm-details)，隐藏 .tags_ul 标签 和 .my_more1(View More)；列表视图(list)显示全部。已在 <style is:global> 加规则：.cbp-vm-view-grid .tags_ul, .cbp-vm-view-grid .my_more1 { display:none; }。
- 预览仍跑在 4322（4321 被旧 server 占）。
## en/products.astro 列表视图精修（13:39）
- 用户要求三项：① 列表态图片高度小一些；② 图片和标题一样可点击跳转；③ View More 加点击外框。
- 改动：.cbp-vm-view-list .cbp-vm-image 加 height:160px（原 width:240 自然高~180）；新增 .cbp-vm-image a { display:block; width:100%; height:100% }；12 张卡图片用正则包裹 <a href=
## View More 按钮改为实心橙按钮（13:49，更正上条）
- 用户要求：更好看、无箭头、宽度合适、橙色按钮、白字。.my_more1 由「橙色描边按钮」改为**实心橙按钮**：ackground:#ff6b1a; color:#fff; border:none; border-radius:6px; padding:8px 22px; justify-content:center，hover ackground:#d9550f。
- 12 处 View More &rarr; 的箭头实体已删（Edit replace_all，无残留 &rarr;/2192）。
- 品牌橙 token：--color-brand-orange: #ff6b1a（dark #d9550f / light #ff8c4a），定义在 src/styles/global.css:115。
- 复验：12 个 my_more1 链接、无箭头、橙底白字 hover 全 True。预览 http://localhost:4321/en/products 。
## 列表视图：按钮左对齐 + 图片满高（13:52）
- .my_more1 加 lign-self: flex-start（之前作为 column flex 子项被 stretch 拉满宽→文字居中看着像通栏按钮；现在贴合内容、靠左）。
- .cbp-vm-view-list .cbp-vm-image 去掉 height:160px，改 width:240px; flex-shrink:0; aspect-ratio:auto; align-self:stretch（在 row/stretch 的 .cbp-vm-li 里上下满高，img object-fit:cover 铺满）。
- 再次踩 watcher 漏更新（served CSS 仍是旧 height:160）→ 重启 dev 后复验通过。**规律：用 Edit 工具改 .astro 后，dev 也经常不重编译，改完必须核 served CSS，不对就 kill 端口重启。**
## 更正：列表图片改为限高 150 + 上下居中留白（14:01）
- 用户明确否定上一步「撑满行高」：要「左侧图片高度限制、变小一些、上下有间距」。
- 最终规则：.cbp-vm-view-list .cbp-vm-image { width:240px; height:150px; flex-shrink:0; aspect-ratio:auto; align-self:center; margin:1rem 0; }（align-self:center 上下居中 + margin 1rem 0 保证上下留白）。
- 又遇 dev 502/不重编译，kill 4321/4322 → 重启 dev 后复验通过。
## en/products.astro 左栏重构（14:41）= 4 项
1. Product Categories 标题改成 38px 表头条（h-[38px] flex items-center px-3 rounded-lg bg-gray-50 border border-gray-200，class left_h_bar），高度对齐右侧 .cbp-vm-icon（38px）。
2. 类目项 Hybrid Var Compensator → Advanced Static Var Generator (ASVG)。
3. 新增「Filter」卡（.filter_card）：Voltage 勾选 220V/400V/480V/690V/800V（5 个），Current 勾选 8~20A/30~50A/50~200A（3 个），均为原生 checkbox，accent-color:#ff6b1a（在 is:global 加 .filter_card/.size_card input[type=checkbox]{width:16px;height:16px;accent-color:#ff6b1a}）。
4. 新增「Dimensions」卡（.size_card）4 个尺寸 checkbox：340*410*45mm / 380*4225*88mm / 500*550*130mm / 500*550*175mm。
- ⚠️ 380*4225*88mm 疑似笔误（多一位数，其他都是 W*H*D 三位），已按用户原文保留，待确认是否为 380*425*88mm。
- 左栏顺序：Product Categories → Filter → Dimensions → New Products(hot_left，表头仍是普通 h3 未统一)。
- dev 又崩：日志报 Failed to load url astro:server-app.js（Vite HMR 断）→ 502；kill 端口重启后恢复。规律：Edit 后必须核 served HTML，502/不更新一律重启。
## en/products.astro 筛选功能实装 + 4列 + 左边距（15:27）
1. 尺寸笔误修正：380*4225*88mm → **380*425*88mm**（用户确认）。
2. **筛选真正可用（已 Playwright 实测）**：12 张产品卡加 data-v/data-a/data-s；12 个 checkbox 加 data-filter(v|a|s) + alue；script 里加筛选 IIFE（同组内 OR，跨组 AND；全不勾=全显）。
3. 网格改 4 列：base1 / ≥640 2 / ≥768 3 / ≥1024 **4**（gap 1.5→1.25rem）。
4. 左边距调小：section 容器 px-[clamp(24px,8vw,160px)] → pl-[clamp(12px,2vw,40px)] pr-[clamp(24px,8vw,160px)]。
- ✅ Playwright 无头实测（1440 视口）：初始 12 卡 / grid 列数=4 / 勾 220V→2 / 勾 50~200A→5 / 勾 220V+8~20A→1 / 取消→12。截图 C:\\Users\\lenovo\\WorkBuddy\\filter_test.png。
- ⚠️ 数据是占位：每张卡的电压/电流/尺寸是我按品类合理分配的，非真实参数，待替换。
- **Playwright 跑法坑（重要）**：venv=C:\\Users\\lenovo\\.workbuddy\\binaries\\python\\envs\\default\\Scripts\\python.exe（浏览器二进制已装）。脚本①必须放**纯 ASCII 路径**（中文 argv 会挂）；②结果要**边跑边写+flush+fsync**；③结尾用 os._exit(0) 强制退出——rowser.close()/with 退出会在沙箱里硬崩（exit 1 且不写结果）。
  - 本轮新踩：`browser.close()` 崩在**调用那一刻**（不是退出时），所以写在 close 之后的落盘代码**根本不会执行**。日志最后一行是 "screenshot ok"，下一行永远等不到。**结论：脚本里不要出现 browser.close()，跑完直接 os._exit(0)。**
  - 另一个坑：**PowerShell 工具里内联调用 python 不落盘**（`& $py script.py`、内联 `Start-Process` 都不产出文件，退出码还显示 0）。必须写成 `.ps1` 文件、用 `pwsh -NoProfile -File xx.ps1` 跑，并 `-RedirectStandardOutput/-RedirectStandardError` 重定向到文件再 Read。另：PowerShell 中 `$pid` 是只读自动变量，`foreach ($pid in ...)` 会报错，改用 `$procId`。

## en/products 左栏合并 + Contact Us 卡 + 第一个内页（17:09）

### 1. Dimensions 并入 Filter 卡（不再是独立 .size_card）
- `.size_card` 整块删除，4 个尺寸 checkbox 移入 `.filter_card`，与 Voltage / Current **同级别**（第三个 `<p>` 小标题 + `.space-y-2`）。
- scoped CSS 选择器随之从 `.filter_card, .size_card input...` 简化为 `.filter_card input[type="checkbox"]`。验证 `size_card` 计数 = 0。

### 2. 新增 Contact Us 卡（Filter 与 New Products 之间）
- `.contact_card`：WhatsApp 链接 `https://wa.me/8618818994346`（target=_blank + rel=noopener）、邮箱 `mailto:leo.li@chitek-inno.com`（带 svg 图标，与 Sidebar 联系信息一致）、`<textarea data-inquiry-summary readonly>`（4 行，灰底）、底部 `<a class="my_more1 w-full justify-center" href={lp('/contact')}>Inquire Now</a>`。
- 筛选 JS 的 `apply()` 里同步写入摘要：`summary.value = lines.join('\n')`，格式 `Voltage: 220V` 或 `Current: 50~200A\nDimensions: 500*550*175mm`；清空勾选则置空。

### 3. 新建第一个产品内页 `src/pages/en/products/sic-ultra-ahf.astro`
- 结构模仿 `F:\projects\cloned-sites\coepowers-ahf\...\product\active-harmonic-filter\index.html`，**骨架沿用本站 `en/products/active-harmonic-filter.astro` 的既有详情页模板**（这样 Nav/Hero/Footer/Sidebar 自动与原汇总页一致，因为都走 `BaseLayout`）。
- 组成：3 段 JSON-LD（BreadcrumbList / Product / FAQPage）→ Hero（`products-hero-bg.webp` 底图 + 深色遮罩 + 左侧文案 + 右侧产品图）→ 面包屑 → 吸顶分节小导航（10 项）→ 6 宫格速览参数 → 10 个编号分节（01 Overview / 02 参数表 16 行 / 03 Solutions / 04 Sizing / 05 Features / 06 Performance / 07 Applications / 08 FAQ 6 条 `<details>` / 09 Why CHITEK / 10 Delivery）→ 橙色 CTA（Request a Quote + Chat on WhatsApp）→ Related Products 4 卡。
- 每个分节都有 `scroll-mt-[150px]`（避开 72px 顶栏 + 吸顶小导航）。

### 4. SiC 卡片三处链接全部指向内页
- 图片 `<a href={lp('/products/sic-ultra-ahf')}>`、标题 `<a href={lp('/products/sic-ultra-ahf')}>`、`View More` `<a href={lp('/products/sic-ultra-ahf')}>`（原来全是 `#`）。`lp()` 在本地会把 `/products/...` 拼成 `/en/products/...`。

### 5. ⚠️ 发现两个项目级既有问题（非本轮引入）
- **`public/assets/images/products-hero.webp` 是一张近乎全白的空白图**（右下角还有「豆包AI生成」水印）。已有 AHF 内页 hero 也用它 → 那个位置一直是白板。本轮已把 SiC 内页的 hero 与 Delivery 区图片**换成真实产品图 `index-product-ahf.webp`**，视觉正常。**AHF 内页是否也换，待用户决定。**
- **英文专属详情页的 hreflang 指向不存在的翻译页**：`/en/products/sic-ultra-ahf` 与 `/en/products/active-harmonic-filter` 都只存在于 en，但 BaseLayout 默认 `alternates=true`，于是输出 zh/pt/es/ar 的 `/products/...` 链接（这些页面不存在 → 5 条坏 hreflang）。修法一行：`<BaseLayout ... alternates={false}>`。本轮**未改**（保持与既有 AHF 页一致），因为「是补翻译还是关 hreflang」属站点级决定，交用户定。

### 6. 新增可重跑回归脚本 `_verify_products_e2e.py`（项目根，按既有 `_smoke*` 命名约定）
- 覆盖 37 项断言：网格 4 列 / grid 隐藏 tags+ViewMore / 列表图片 240x150 上下 16px 留白 / ViewMore 橙底白字 / 筛选同组 OR 跨组 AND 清空复原 / 摘要写入 / Contact Us 三链接 / SiC 三处链接 / 点进内页后 H1·分节数·小导航·FAQ·参数行·Footer·Sidebar·JSON-LD·hero 图真实加载。
- 内置 `_ensure_playwright()` 自愈（缺包自动 re-exec 到共享 venv），用户直接 `python _verify_products_e2e.py` 即可（需 dev 在 4321，可用环境变量 `CHITEK_BASE` 覆盖）。
- ✅ 实测 **37/37 PASS**。
- 归档产物：`C:\Users\lenovo\WorkBuddy\00_中转站\chitek-sic-ahf\`（7 张截图 + 回归输出）；64 个临时探针文件已移入 `F:\trash\20260917-170914_chitek-verify-probe`。

### 7. 验证过程要点
- 本轮再次确认：**改了 .astro 后必须核 served HTML**。这次 Edit 之后 dev 确实热更新了（图片替换后 `products-hero.webp` 计数由 12 → 0，已验证），但改动批次较大时直接 kill 端口 + 重启最稳。
- 页面 200 校验：`/en/products`、`/en/products/sic-ultra-ahf`、`/en/products/active-harmonic-filter` 均 200。
- 待办：SiC 内页目前只有英文；用户说「先做第一个内页」→ 其余 11 张卡的内页后续按同一模板复制。

## 追加：SiC 内页顶部改成 WooCommerce 式「面包屑 + 左文右图」（17:20）

- 用户给了一张参考截图（SVG 产品页）：要「顶部左边描述」按那个格式，**而且没有背景**，**面包屑在最顶部**。
- 回看参考页 `coepowers .../active-harmonic-filter/index.html` 结构确认 = **WooCommerce 单产品页**：`woocommerce-breadcrumb` 在**最顶**（独立一行容器）→ 两栏（左图右文）→ 右侧栏依次是 `product_title`(h1) → `product_meta` 的 `Category` / `Tags` → `short-description`（一堆裸 `<p>` 规格行，**无项目符号**）→ 一个 `Get A Quote` 按钮。
- 已改造 `sic-ultra-ahf.astro`：
  1. **删掉整个深色 hero**（含 `products-hero-bg.webp` 底图 + `bg-black/60` 遮罩 + eyebrow + 副标题 + 双按钮）。
  2. **面包屑提到最顶部**，`bg-white border-b`，并加 **`mt-[72px]`**——因为 `Nav.astro` 是 `fixed top-0 h-[72px]`，**不自己让出 72px 就会被导航压住**（实测 `bc_top` 从 0 → 72 才正确）。
  3. 新增 **`[data-product-top]`** 区块（白底、无背景图）：左栏 = h1（关键词堆叠式长标题）→ `**Category** ...` → `**Tags** ...` → 6 行裸 `<p>` 规格（220V/400V/480V/690V、Frequency 50/60HZ、Three phase、Connection、Warranty、CE Approved）→ `Advantage Feature:` + `<ol class="list-decimal list-inside">` 4 条 → 橙色 `Get A Quote`；右栏 = 产品图（圆角边框 + `bg-[#fafbfc]` + `object-contain max-h-[460px]`，不再 `object-cover` 裁切）。
  4. 顺序变为：面包屑 → 顶部产品块 → 吸顶分节小导航（`top-[72px]`）→ 6 宫格速览 → 10 个编号分节 → CTA → Related。
- 与参考图的**有意偏差**（已告知用户，待其定）：① 标题用站点 `text-text-dark`，**没用图里那个深紫色**；② 参考原文是 `Advantage Feather:`（拼写错误），本站英文页写成 **`Advantage Feature:`**；③ 参考页那个 SVG 产品的文案（30-500kvar / PFC）**没有照抄**，按 SiC AHF 写成对应参数；④ 顶部只留**一个** `Get A Quote` 按钮（对齐参考页），原来的 `Learn More` 已删。
- 回归脚本同步更新（`_verify_products_e2e.py`）：H1 断言换成新长标题，新增第 7 组 8 项顶部块检查（面包屑 y=72 / 面包屑先于 h1 / 无 `.bg-black/60` / `[data-product-top]` 存在 / 2 个加粗标签 / `<ol>` 4 项 / CTA 文案 / 全页 hero 背景图仅剩页尾 CTA 1 处）→ ✅ **45/45 PASS**。
- 截图归档 `C:\Users\lenovo\WorkBuddy\chitek_preview\sic_detail_top_new.png`；临时文件移入 `F:\trash\20260917-171500_chitek-top-rework`。
- 遗留：6 宫格速览参数（RATED VOLTAGE / CAPACITY / THDi / RESPONSE / EFFICIENCY / TOPOLOGY）与顶部规格行**略有重复**，用户未要求删，暂留待确认。

## siC-ultra-ahf 去掉 Overview 区 + 分节导航改「左侧固定侧栏」（17:19）

- 需求：删掉 OVERVIEW 区域；把原来的**顶部横向吸顶小导航**改成**左侧可点击的竖向导航栏**。
- 改 `src/pages/en/products/sic-ultra-ahf.astro`（脚本 `_fix_sic_sidebar.py`，全部 `assert`，备份 `.workbuddy/tmp/sic-ultra-ahf.bak.astro`）：
  1. **删 Overview 分节**（`<!-- 01 Overview -->` … `</section>`）。
  2. **布局改为两栏 flex**：`<div class="lg:flex">` 包住全部内容；左列 `<aside class="hidden lg:block lg:w-[224px] shrink-0 border-r">`，其内 `nav.sticky top-[72px] max-h-[calc(100vh-72px)] overflow-y-auto`（只有它吸顶，才能到页尾自然结束、不压 Footer）；右列 `<div class="flex-1 min-w-0">` 装原内容。左列用 `lg:` 前缀 → 手机自动隐藏。
  3. **桌面侧栏**保留 9 项（Technical Parameters…Delivery，去掉 Overview），每项 `class="js-secnav …"`。
  4. **手机保留横向吸顶导航**（`lg:hidden sticky top-[72px]`，同一个 9 项）→ 移动端不丢导航。
  5. **分节编号整体上移一位**（02→01 … 10→09），eyebrow 文案 + HTML 注释同步改。
  6. `scroll-mt-[150px]` → `scroll-mt-[120px]`（9 处；桌面已无横向小导航）+ 注入 **IntersectionObserver 滚动高亮脚本**（`.js-secnav` 命中即加 `text-brand-orange border-brand-orange`、去 `text-text-muted border-transparent`；`rootMargin '-30% 0px -60% 0px'`）。
- 为什么用「flex 两栏 + 内层 sticky」而非「fixed 侧栏 + 内容左内边距」：**fixed 会盖住页尾 Footer**；flex 里 aside 与 main 同高、sticky 子元素滚动区间止于 main 底部 → 到 Footer 自然停住。
- 验证：served HTML `js-secnav=9` / `lg:flex=1` / 手机 nav=1 / `01 / OVERVIEW`=0 / `01 / TECHNICAL PARAMETERS`=1 / `09 / DELIVERY`=1；Playwright 截图（1440 桌面顶/滚动 + 390 手机）确认侧栏、滚动高亮（滚到 Applications 时该条变橙 + 左橙边）、手机横向导航均正常。dev 重启于 4321（task VXAE1x）。
- ⚠️ 仅 en 站。zh/es/ar/pt 若有同名内页未同步。顶部 6 宫格速览仍在（未动）。

## en/products 筛选卡：四组均改一行两个 + 新增 IP Rating 组（17:30）

- 需求：Filter 的 Voltage 改「一行两个」；Current 下面新增 IP20/IP30/IP40/IP54/IP55/IP65 共 6 项，同样一行两个。
- 改 `src/pages/en/products.astro`（脚本落盘 + 全量 `assert`，**没用 Edit**，因为要同时改 12 张卡）：
  1. **Voltage / Current / Dimensions 三组的容器**由 `class="space-y-2"` → **`class="grid grid-cols-2 gap-x-2 gap-y-2"`**。
     - Current 和 Dimensions 用户没点名，但为视觉一致**一并改了**（Voltage 两列 / Current 单列会很难看）。
  2. **新增 IP Rating 组**插在 Current 与 Dimensions 之间，6 个 checkbox，`data-filter="i"`，同样两列。装了「IP Rating」小标题（用户只给了选项没给组名，属合理推断，已告知）。
  3. **12 张 `.cbp-vm-li` 补 `data-i="…"`**（否则勾 IP 会把 12 张全筛没）。按柜式/挂式/户外合理分配：柜式 IP20,IP30（±IP40）；挂壁式（Wall mounted）IP54,IP55；Filtration System / Compensation Filter 带 IP65。**6 个选项每个都至少有 1 张卡命中**（不会出现点了没结果）。**占位数据，待业务替换。**
  4. **筛选 JS 同步**：`labels` 加 `i: 'IP Rating'`；`active` 加 `i: []`；两处 `['v','a','s'].forEach` → `['v','a','i','s'].forEach`。摘要框因此会显示 `IP Rating: IP65` 这样的行。
- **落盘手法**：Python 脚本按**行号降序**改（先改 12 张卡的高行号，再改筛选卡，插入 IP 组在最后）→ 避免插入导致行号漂移。脚本自带 9 条计数断言（`data-filter="i"`=6 / `data-i="`=12 / 四个两列容器=4 / `space-y-2`=0 …），任一不符就整段不写盘。
- ✅ Playwright 实测（**55/55 PASS**）：四组列数 `2,2,2,2`；每组行数 `3,2,3,2`（Voltage5 → 3 行、Current3 → 2 行、IP6 → 3 行、Dim4 → 2 行）；勾 IP65 → 2 张；IP65+IP55（同组 OR）→ 3 张；再叠 220V（跨组 AND）→ 1 张；清空 → 12。截图 `chitek_preview\products_filter_2col_ip.png`。
- ⚠️ **踩坑复盘**：改完先跑回归，内页两项断言挂了（分节 9≠14、小导航 18≠10）。查 DOM 才发现**用户在此期间自己改过 `sic-ultra-ahf.astro`**（加左侧吸顶竖向分节栏、删 Overview 分节 → 9 分节 / 2 个同名 nav 各 9 项）。**教训：回归脚本的期望值会随用户的手改而失效；断言失败先查 DOM 和文件，别急着改代码。** 已按新结构更新断言（新增 `js-secnav`=9、两个 nav 各 9 项）后 55/55 全过。
- 临时文件移入 `F:\trash\20260917-173000_chitek-filter-ip`。

## 删掉内页「6 宫格速览」（17:35）

- 用户一句话需求：删掉 6 宫格速览（RATED VOLTAGE / CAPACITY / THDi / RESPONSE / EFFICIENCY / TOPOLOGY）。
- 改 `src/pages/en/products/sic-ultra-ahf.astro`：整块 `<!-- Key specifications at a glance -->` + 其 `<section class="bg-white py-7 border-b">`（含 `grid-cols-2 md:grid-cols-3 lg:grid-cols-6` 与 6 个卡片）删除。
- 删后版面：手机横向导航 → 直接进 `01 / TECHNICAL PARAMETERS`（`bg-[#fafbfc]`），视觉连贯，无空洞。
- 回归脚本加断言「6 宫格速览已移除 (无 `grid-cols-6`)」→ ✅ **56/56 PASS**。
- 该 6 项数据并非丢失：同一批指标在 `02 Technical Parameters` 参数表里都有（Rated Voltage 220–690V / Capacity / THDi / Efficiency…），JSON-LD Product 的 `additionalProperty` 也仍在。

## SiC 内页左侧栏细化：不入侵面包屑 + PAGE GUIDE + 收窄 + 滚动高亮对应文字（17:43）

- 用户一句话需求（三点）：① 左侧栏不要压到上面的面包屑；② `On this page` 改 `PAGE GUIDE`；③ 侧栏宽度小一点；④ 滚动到哪个分节，侧栏对应那条要**高亮文字**。
- 改 `src/pages/en/products/sic-ultra-ahf.astro`：
  1. **面包屑搬出 flex 容器**。原来面包屑在 `<div class="lg:flex">` **内部**、排在 `<aside>` 之后 → 其左边界被侧栏挤到 x=224（用 Playwright 探针确认）。现在把整个 `<nav aria-label="Breadcrumb">` 移到 `lg:flex` **之前**，成为整行通栏。实测 left=0、宽度==`documentElement.clientWidth`。
  2. **侧栏收窄** `lg:w-[224px]` → **`lg:w-[188px]`**，内边距 `px-4` → `px-3`。
  3. 小标题 **`On this page` → `PAGE GUIDE`**。
  4. **滚动高亮从 IntersectionObserver 换成确定性 `pick()`**：原来 `entries.forEach` 在「同时有两节与 rootMargin 相交」时会**高亮错条目**。新逻辑 = 取「`getBoundingClientRect().top - (72+120) <= 0` 的**最后一个**分节」，再加一条兜底：滚到页面底部时**强制选中最后一节**（末节常常永远到不了判定线）。用 `requestAnimationFrame` 节流。
  5. 高亮样式：不再去改 Tailwind 类，改为 JS `classList.toggle('is-active')` + `<style>` 里 `:global(.js-secnav.is-active)`（橙字 `var(--color-brand-orange,#ff6b1a)` + 橙左框 + `rgba(255,107,26,.08)` 淡橙底 + `font-weight:600`）。用 `:global()` 是 Astro scoped 样式对「运行时加的类」的固定要求（见 MEMORY.md）。
- ✅ 回归脚本扩到 **68/68 PASS**（新增第 8 组 12 项：面包屑通栏/不在 flex 内/left=0、侧栏 188px、PAGE GUIDE 文案、9 条链接、sticky 生效、滚到 #faq 时高亮条数=1 且 href="#faq" 且 `rgb(255,107,26)/600`、沉底命中 #delivery、窄视口 900px 侧栏 display=none）。
- 视觉确认截图：`C:\Users\lenovo\WorkBuddy\chitek_preview\02-sic-top.png`（面包屑通栏 + 左侧 PAGE GUIDE + 右侧产品图，顶部无背景图）、`03-sic-sidebar-faq-active.png`（FAQ 高亮橙字+淡橙底）、`04-sic-sidebar-parameters-active.png`（Technical Parameters 高亮）。
- 清理：`.workbuddy/tmp` 下 57 个遗留探针文件（含旧 `sic-ultra-ahf.bak.astro`）移入 `F:\trash\chitek-tmp-20260917-174308`。
- ⚠️ 本轮再次踩到：**本会话 Bash 工具完全不可用**（`grep`/`ls`/`head` 全部 `command not found`，`dirname` 缺失 → 每次退出码 127）。凡是 shell 类操作一律走 PowerShell 工具 + `.ps1` 文件，或直接用 Grep/Glob/Read 专用工具。
- ⚠️ 仍仅 en 站；zh/es/ar/pt 未同步。遗留两处既有缺陷未动：① AHF 内页 hero 仍用空白图 `products-hero.webp`；② en 专属详情页 hreflang 指向不存在的翻译页（`alternates={false}` 一行可修）。