# 2026-07-13 工作日志

## 新增中文新闻文章（百家号转载 → 网站 news 页）

- 源文件：`F:\下载\web-crawler\outreach\templates\百家号文章\2026年有源滤波器行业从业者从业情况自述.txt`
- 新建文章：`src/pages/news/apf-industry-practitioner-2026.astro`
  - 标题《2026年有源滤波器行业从业者从业情况自述》，category=行业动态，date=2026-07-13，author=CHITEK 编辑部，readTime=9 分钟阅读
  - 内容为公司（深圳市启点新能源科技有限公司）有源滤波器业务自述，整理为 B2B 工业风结构化长文（产品矩阵 8 类 + 7 项专利表 + 合规实测 + 服务 + 生产 + 行业判断）
  - frontmatter / Schema.org Article / Sidebar 均按现有模板（sic-ahf-launch.astro）结构
- 列表页：`src/pages/news.astro` 首位插入新卡片（链接 /news/apf-industry-practitioner-2026）
- 配图：用 `index-product-ahf.webp`（blog 目录仅有 sic-ahf-launch.webp，无专属图）；ogImage 用 `https://chitek-inno.com/assets/images/index-product-ahf.webp`
- 未提交 git（按 CLAUDE.md 规范，默认不提交）

## 环境约束（重要，避免下次重复踩坑）

- 本环境 `npm run build` / `astro check` 会在收尾阶段删除 `dist/.prerender/.vite/`（vite 临时缓存目录），触发 WorkBuddy 的 `genie-safe-delete` 批量删除守卫（阈值 50 文件/turn）被拦截，导致 build/check 无法完整跑通。
- 现象：`[safe-delete][SAFE_DELETE_BULK_REJECTED] targets: dist\.prerender\.vite\`，用户拒绝删除后命令退出。
- 即便 `build.noClean:true` 也无效（Astro 仍会删该临时目录）。
- 影响：无法在本地完成完整构建验证。代码本身基于已验证模板，结构一致。需用户批准删除 dist 临时目录或在其本地无此守卫环境构建。
- 临时安装的 `@astrojs/check` + `typescript` 用 `--no-save` 方式（未改 package.json）。

## 多语言扩展（en/es/ar 同步新文章）

- 为上文中文文章同步创建 en/es/ar 三语版本（葡语 pt 按 CLAUDE.md 无新闻，跳过）：
  - `src/pages/en/news/apf-industry-practitioner-2026.astro`（import `../../../`，lang=en，链接 `/news/...` 与 en 模板一致）
  - `src/pages/es/news/apf-industry-practitioner-2026.astro`（lang=es，链接 `/es/news/...`、`/es/products#...`）
  - `src/pages/ar/news/apf-industry-practitioner-2026.astro`（lang=ar，链接 `/ar/news/...`、`/products#...` 沿用 ar 模板写法）
- 三语言列表页首位各插入新卡片：
  - `src/pages/en/news.astro`（文件形式）
  - `src/pages/es/news/index.astro`（目录形式，非 news.astro）
  - `src/pages/ar/news/index.astro`（目录形式）
- 注意：es/ar 新闻列表页是 `news/index.astro` 目录形式，en/zh 是 `news.astro` 文件形式——Glob 校验 `news*.astro` 匹配不到 es/ar，需用 `news/index.astro`。
- 配图/ogImage 同中文版（index-product-ahf.webp）。
- 仍未跑完整 build（safe-delete 守卫约束）；靠模板一致性 + Glob 校验确认落地。
- 未提交 git。

## 首页 HERO 轮播改造

- 需求：把 `public/assets/images/index-hero-bg.png` 转 webp 并改名为 `index-hero-bg1`，首页 HERO 改成「原图 + 新图」轮播。
- 转换：环境无 PIL/ImageMagick，用 PATH 中的 ffmpeg（`/d/ffmpeg-7.1`）`libwebp` 编码。
  - 命令：`ffmpeg -y -i .../index-hero-bg.png -vcodec libwebp -quality 85 -compression_level 4 .../index-hero-bg1.webp`
  - 结果：1672×941，2.1MB PNG → 177KB WebP。
- 改动文件：`src/pages/index.astro`（仅中文首页）
  - HERO 单 `<img>` 替换为 `.hero-carousel` 容器 + 两张 `.hero-slide`（index-hero-bg.webp 默认 active、index-hero-bg1.webp）+ 底部 `.hero-dots` 两个可点击指示点。
  - `<style>` 纯 CSS：`.hero-slide` 透明度过渡 1200ms 淡入淡出；`.hero-dot` 圆点 + active 变橙色加宽（未用内联 style，符合规则）。
  - `<script>`：每 5s 自动切换 + 点击圆点跳转并重置计时器。
- 预览：astro dev 在本环境未绑端口（疑似卡依赖优化），改用 `npm run build` 重建 dist（safe-delete 守卫本次自动 escalation 放行），由已有的 3001 静态服务器提供更新后首页。
- 已验证：dist/index.html 含 index-hero-bg1.webp、hero-slide/hero-dot 结构；3001 首页与新图均 200。
- 未提交 git。

## 英文首页 Company intro 区下方新增 4 个动态数字（仅 en 测试）

- 需求：只在英文版 `src/pages/en/index.astro` 测试，在 ABOUT（id=about，即 COMPANY INTRO）区下方加 4 个滚动递增数字计数器：
  - 2019 → Established in
  - 2700+ → Company Area
  - 30+ → Intellectual Properties
  - 20+ → Export Countries
- 实现：
  - 在 ABOUT `</section>` 之后、PRODUCTS 之前插入新 `<section id="stats">`，深色底 `bg-[#0a1322]` + 顶部 `border-t border-white/10`，grid 4 列（md:grid-cols-4，移动端 2 列）。
  - 每个数字包 `<span class="stat-number" data-target="N" data-duration="Ms">0</span>`，带 "+" 后缀的用独立 span；数字用 `font-barlow-condensed` 大号橙色。
  - `<style>` 加 `.stat-number { font-variant-numeric: tabular-nums; }`（等宽数字防抖动）。
  - `<script>`：IntersectionObserver（threshold 0.4）触发 requestAnimationFrame 计数动画（easeOutCubic，到视口只播一次 unobserve）。
- 未改 zh/es/ar；用户明确"只改英文版测试"。
- 重建 `npm run build` 成功（75 页），dist/en/index.html 含 4 个 stat-number + 4 标签；/en/ 200。
- 未提交 git。

### 迭代：可重复播放 + 小数字放慢（en）
- 改 IntersectionObserver 逻辑：不再 unobserve；进入视口播放，离开视口 cancelAnimationFrame + 重置 textContent='0'，下次进入重新递增（可反复播放）。用 el._rafId 存 rAF 句柄避免叠加。
- 30+ 与 20+ 的 data-duration 从 1400 → 2600ms（增长更慢更明显）；2019 保持 1600、2700 保持 1800。
- 重建验证：dist duration="2600" ×2、_rafId ×8、IntersectionObserver 在。

### 迭代：英文首页 HERO 加 index-hero-bg1 轮播 + 主题标语（en）
- 需求：英文首页新增 index-hero-bg1 轮播图，主题文案 "Facilitate harmonious coexistence of energy and ecology"。
- 改动 `src/pages/en/index.astro`：
  - HERO 单 `<img>` → 双图轮播 `.hero-carousel`（原 index-hero-bg.webp 默认 active + index-hero-bg1.webp）+ 底部 `.hero-dots` 两个指示点（与中文首页实现一致）。
  - HERO 内容区 h1 上方新增 eyebrow 主题标语："Facilitate harmonious coexistence of energy and ecology"（brand-orange 小字大写 + 字距）。
  - `<style>` 补 .hero-slide 淡入淡出(1200ms) + .hero-dot 样式；`<script>` 补 HERO 轮播逻辑（5s 自动切换 + 点击圆点跳转重置计时）。
- 注意：中文首页早已加轮播但无此主题标语；英文此次未做"图文同步切换"，主题标语为固定 eyebrow 覆盖在两图上。
- 重建成功（75 页），dist/en/index.html 含 hero-slide / index-hero-bg1.webp / 主题文案；/en/ 200。
- 未提交 git。

### 微调：小数字末跳区间（en）
- 用户反馈 30+/20+ 末跳(29→30、19→20)拖沓。根因 easeOutCubic 末段极慢，小数字末跳占大半时长。
- 改 animateCount：`target <= 50` 用线性匀速(p)，否则保留 easeOutCubic。小数字末跳与全程间隔一致、不再拖；大数字(2019/2700)观感不变。
- 重建成功，dist/en 含 `Math.pow(1-d,3)` 证明 cubic 分支仍在（三元条件压缩后保留）。

### 工作流优化：改 dev 实时预览，不再每次 build
- 用户要求不必每次修改都 `npm run build`（太慢，且触发 safe-delete 守卫需确认）。
- 改用 `npm run dev -- --host --port 4321` 常驻后台预览；首次启动 vite 依赖优化约 1 分钟才绑端口（非卡死）。dev 直接读 src，热更新，改完刷新即见。
- 本轮额外改动（en）：删 COMPANY INTRO 顶部大标题；删 Solution Insights 三卡片图标（src 已改，经 dev 验证 COMPANY INTRO 与 w-14 h-14 mb-8 均=0）。
- 之后预览统一用 http://127.0.0.1:4321/en/；build 仅在出产物/部署时跑。

### HERO 轮播改为「图文同步切换」（en）
- 用户澄清：`index-hero-bg1.webp` 为第 1 页，专属主题仅 "Facilitate harmonious coexistence of energy and ecology" 一句、无其它内容；第 2 页为原首图，标题/副标题/按钮保持不变。
- 实现：两张 img（bg1 首、bg 次，bg1 为 active）+ 两个 `.hero-text` 面板（absolute inset-0，opacity 淡入淡出 + pointer-events 切换），JS heroShow 同步切换 hero-slide / hero-text / hero-dot。
- 面板 0 只放主题大标题；面板 1 保留原 OEM 文案与两个按钮。经 dev 验证 hero-text ×2、bg1 为首帧 active、主题文案在。
- 未提交 git。

## 英文产品页「Four Product Series」卡片左右向中间聚拢入场动画（⚠️ 已回退）

> ⚠️ 此条为**错误目标**：用户本意是英文**首页** `en/index.astro` 的 Product Series 区，不是产品页。经用户澄清「我们现在只调试英文界面」并确认目标为首页后，本产品页的全部改动已 `git checkout HEAD -- src/pages/en/products.astro` 回退（series-card 改动、`.series-card` CSS、tab 重放脚本均清除，仅保留用户既有产品页逻辑）。下方「英文首页 Product Series 区」条目为最终保留方案。
- 实现：
  - 四张产品卡（Standard/SiC/Distributed/Anti-Pollution，AHF 与 SVG 两个 tab-panel 各 4 张，共 8 张）加 `series-card reveal` 类。
  - `<style>` 新增 `.series-card`：初始 `opacity:0`；移动端 `translateY(28px)` 淡入上移；`>=640px`(2列) 奇数卡从左侧 `translateX(-44px)`、偶数从右侧 `+44px`；`>=1024px`(4列) 第1/2张从左侧 `-52px`、第3/4张从右侧 `+52px` → 视觉上向中间汇聚。`.series-card.reveal.visible{opacity:1;transform:none}` 收尾。nth-child 错位 0/90/180/270ms 形成波浪。含 `prefers-reduced-motion` 兜底（直接显示）。
  - 原 grid 容器上的 `reveal` 移除（改由卡片各自承担揭示），避免整块与逐卡双重动画。
  - tab 切换脚本新增：激活面板后对其 `.series-card` 先 `remove('visible')` → 强制 reflow → `add('visible')` 重放动画。
- 踩坑：页面存在多个同名 `.tab-panel[data-tab="N"]`（HERO 区与产品系列区各一组）。重放选择器必须限定 `#product-series .tab-panel[data-tab="..."]`，否则会误命中 HERO 面板导致产品系列卡片不显现。
- 验证：Playwright 无头校验（1280 宽=4列）— 滚动到 #product-series 后 4 张 AHF 卡 opacity=1/transform=none/带 visible；点击 SVG tab 后 4 张 SVG 卡同样重放为 visible；控制台无报错。校验脚本已移出项目目录（/tmp）。
- 预览地址：http://localhost:4321/en/products（dev 服务器常驻）。
- 未提交 git。

### HERO 第1页主题字微调（en）
- 用户连续微调英文 HERO 第1页主题字 "Facilitate harmonious coexistence of energy and ecology"：
  - 字体格式对齐第2页（font-barlow-condensed / bold / 白色+橙高亮 / leading-[1.1]）。
  - 保持两行；"Facilitate harmonious coexistence of" 整句一行（clamp(40px,5vw,76px) + max-w-[1200px] + [word-break:keep-all] 防折行）。
  - 首行偏灰：用 `<span class="text-white/75">` 包首行，"energy and ecology" 保持 text-brand-orange。
- 经 dev 验证（4321）生效。未提交 git。

### Solution Insights 卡片改版（en）
- 三张卡片（Hospital / Data Center / Manufacturing）内层由 `flex flex-col justify-between`（标题上、按钮下）改为 `justify-end`（整块内容靠下）。
- 标题+描述外包裹 `border-l-[3px] border-brand-orange pl-4`：标题左侧加橙色小竖线（竖线贯穿标题与描述文本块）。
- 去掉标题 min-h-[2.6em]；标题-描述间距 mb-5→mb-3；Learn more 加 mt-5。
- 经 dev 验证：border-l×3、justify-end 在；页面其余 justify-between 属其他区块，与卡片无关。未提交 git。

### Company Intro 区域整合统计数字（en）
- 用户要求：Company Intro 区卡片上移，增长的数字（2019/2700+/30+/20+）直接显示在该区域，不再单独成区。
- 改动 `en/index.astro`：
  - `#about` 区块去掉 `min-h-[calc(100vh-72px)] flex flex-col justify-center`（内容顶部对齐=整体卡片上移），padding `py-10`→`py-14`。
  - 4 张优势卡内边距 `py-[clamp(16px,3vw,56px)]`→`py-[clamp(12px,2vw,36px)]`（更紧凑、上移）。
  - 删除独立的 `#stats` 深色带 `<section id="stats">`，将其 4 个 `.stat-number` 计数器以普通 `<div>` 形式并入 `#about` 区块底部（共享 about-bg 暗底，无独立背景色/上边框），实现「不新增区域」。
- 验证：dev(4321) 确认 `id="stats"`=0、stat-number×4(HTML)+JS 内 1 处、about 无 justify-center、卡片紧凑 padding×4、/en/ 200。
- 追加：用户要求 about 区域「正好一屏高」。`#about` 改回 `min-h-[calc(100vh-72px)] flex flex-col py-14`（保留顶部对齐=卡片上移；不写 justify-center），统计数字仍在区内。dev 确认该 section 标签含 min-h、无 justify-center。
- 再追加：用户要求 about 内容「整体更居中、向下调整」。给 `#about` 加回 `justify-center`，内容在一屏内垂直居中（从顶部下移到中部）。dev 确认标签含 justify-center。
- 再追加：HERO 第1页首行 "Facilitate harmonious coexistence of" 由 text-white/75 改纯白 text-white。
- 再追加(7/14)：ABOUT CHITEK 标题字号 32px→45px（原 1.4×）。
- 未提交 git。

## 英文首页 Product Series 区左右向中间聚拢入场动画（最终保留方案）

- 需求（经澄清）：英文**首页** `src/pages/en/index.astro` 的 Product Series 区（id=products）三栏布局，内容加载时左右两栏向中间聚拢出现。
- 目标文件：仅 `src/pages/en/index.astro`（en 首页）；zh/es/ar/pt 首页及 en/products.astro 均不动（已回退/未同步）。
- 实现：
  - 三栏 grid 容器去掉整块 `fade-up`（opacity-0 上移），改由三栏各自承担揭示：
    - 左栏（文本）：`fade-up conv-left`
    - 中栏（特性网格）：`fade-up`
    - 右栏（图片）：`fade-up conv-right`
  - `<style>` 新增（纯 CSS，未用内联）：
    - `@media (min-width:1024px)`：`.conv-left{transform:translateX(-64px)}`、`.conv-right{transform:translateX(64px)}` —— 桌面宽屏从两侧外移待揭示。
    - `.fade-up.conv-left.visible, .fade-up.conv-right.visible{transform:translateY(0)}` —— 揭示后回到中心（specificity 0,3,0 压过 `.fade-up.conv-left` 偏移，解决"揭示后仍偏移"的 specificity bug）。
    - `.conv-left,.conv-right{transition-delay:120ms}` —— 略迟于中栏，形成向中聚拢的层次感。
    - `@media (prefers-reduced-motion:reduce)`：`.conv-left,.conv-right{transform:none!important;transition:none!important}` —— 无障碍兜底直接显示。
  - 复用 BaseLayout 全局 `.fade-up` IntersectionObserver（threshold 0.12）触发 `.visible`，无需新增 JS。
- 范围纪律：用户明确「我们现在只调试英文界面」→ 只改 en 首页。早先误同步到 zh/es/ar/pt 首页的 conv 改动（sync-home-converge.py）已用 revert-sync.py 回退；残留的 `</style>` 空白行产物已 `git checkout HEAD --` 清除；en/products.astro 错误目标改动也已回退。最终 git 仅 `en/index.astro` 一处为我方改动，其余 modified 文件均为用户既有工作（news 列表、zh 首页 HERO 轮播等）。
- 验证：served /en/ 含 conv-left/conv-right 类、translateX(±64px) 偏移规则、conv-[left|right].visible 复位规则、id=products 在；此前 Playwright 无头校验（1280 宽）确认滚动到 #products 后左右栏 opacity=1/transform=matrix(1,0,0,1,0,0)（已聚拢到中心）、中栏 visible、控制台无报错。dev 服务器常驻 http://localhost:4321/en/。
- 未提交 git。

### 迭代：向中间靠改为可反复触发（en 首页）
- 用户要求「向中间靠」改成可反复触发（进出视口反复重播），而非只播一次。
- 根因：BaseLayout 全局 `.fade-up` 观察器只 `add('visible')` 从不移除 → 首次进视口后永久保持 revealed，不会重播。
- 改动 `src/pages/en/index.astro`：
  - `<script>` 新增本地观察器，仅作用于 `.conv-left,.conv-right`（不碰全局，全站其它 fade-up 不受影响）：进视口 `add('visible')`、出视口 `remove('visible')`（threshold 0.12 与全局一致）→ 离开复位为偏移+透明、再次进入重新聚拢到中心。
  - `@media (prefers-reduced-motion:reduce)` 规则补 `opacity:1!important`，确保无障碍用户始终可见（不随 visible 类消失/重现）。
- 验证：Playwright 无头（1280 宽）三步断言全过 —— IN VIEW(hasVisible/opacity1/transform居中) → OUT VIEW(无 visible/opacity0/transform translateX(-64px) 偏移) → REPLAY(hasVisible/opacity1/居中)，控制台无报错。脚本已移出项目目录（/tmp/scratch-chitek-20260713/）。
- 未提交 git。
