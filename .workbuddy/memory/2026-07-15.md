# 2026-07-15

## 新闻列表页卡片顺序修复（zh/es/ar → 与 en 对齐）

### 问题
- 之前 zh/es/ar 新闻列表页的 3 篇 2025 年文章排在最前面，2026 年的 8 篇文章排在后面→ 日期不递减，与 en 不一致。
- 原因：当初"3 篇 EN-only 补齐到 zh/es/ar"时，卡片追加到了列表顶部、2026 年文章之前；en 则排在对的位置（2026 之后、2024 之前）。

### 修复
- 脚本提取 en 的 slug 顺序（16 篇）→按同样顺序重排 zh/es/ar 的卡片块。
- 重排后 zh/es/ar 日期顺序与 en 完全一致：
  2026-05-31 → ... → 2026-02-22 → 2025-12-15 → ... → 2025-10-25 → 2024-12-15 → ... → 2024-10-05
- ar 的 3 篇 5 月文章保留阿拉伯文格式（مايو），顺序正确。

## 新闻列表页布局修复（zh/es/ar → 与 en 一致）

### 问题
- 前 8 篇卡片（2026 年）被插在 `<Sidebar>` 和 Hero Section 之间，脱离了 `#articles` 容器，未包裹在 `space-y-6` 内。
- 分页区域缺少外层 `<div class="flex ... gap-2 mt-16">` 容器、`<!-- Pagination -->` 注释和左箭头按钮。

### 修复
- 将 8 张游离卡片从 Sidebar 后区域移到 `space-y-6` 容器内（所有卡片现在都在 `#articles` 区域内）。
- 分页包裹为与 en 一致的结构：添加 flex 容器、gap-2 mt-16、<!-- Pagination --> 注释、左箭头按钮。
- 临时脚本已删除；未提交 git。

## 中文产品页 FAQ 英译中（AHF + SVG 共 20 面板）

### 翻译范围
- **AHF FAQ Tab**（10 个面板）：谐波畸变影响、行业应用场景、响应时间、并联运行、通信协议、使用寿命、变压器要求、恶劣环境、认证合规、调试配置
- **SVG FAQ Tab**（10 个面板）：关键问题、行业应用、SVG vs 电容器、保护功能、寿命与MTBF、安装要求、不平衡负载、新能源集成、待机功耗、通信协议
- 含 SVG vs 电容器对比表格（4 行 x 3 列表头及内容）

### 注意事项
- Python 脚本中 `&mdash;`（HTML 实体）与文件实际使用的 Unicode em dash `—` 不匹配，导致脚本替换失败。剩余 4 个面板（AHF 通信协议/认证、SVG 保护功能/新能源集成/通信协议）通过 Edit 工具手动修复。
- 脚本中 `svg_replacements[-1]` 意外覆盖了 SVG Panel 8（新能源集成）条目，后手动补回。
- 临时脚本已删除；未提交 git。

## products.astro 结构调整（09:53-10:1x）
- 用户要求："products总页面 Four Product Series 区域放在 hero 下面"。
- 三轮移动（均用 python 脚本，锚点定位 section，保留 tab 联动）：
  1. ⚠️ 误移 AVR Coming Soon（隐藏板块，视觉无影响）→ 用户纠正："移动的是 AHF 和 SVG 的"。
  2. 移 Value Proposition section 到 Four 之后 → 用户仍反馈 hero 下是 Key Feature。
  3. 移 Key Features section(id=features) 到 Four 之后 ✅ 命中真正挡路的区块。
- 真实完整结构（移动前）：Hero(标题) → Key Features(id=features) → Value Proposition → Four Product Series(AHF+SVG 卡片) → AVR → HMI。
- 最终顺序：Hero → Four Product Series → Key Features → Value Proposition → AVR → HMI。Four Product Series(AHF+SVG 卡片) 紧贴 Hero 标题下方。
- dev(4321) 验证：HTTP 200，DOM 顺序 Four<Key<Pain<HMI=True（Four 191498 < Key 254621 < Pain 286020 < HMI 329310）。
- 备份：products_astro_backup.txt / _backup2.txt / _backup3.txt（temp）。未提交 git。
