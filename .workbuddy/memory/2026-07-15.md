# 2026-07-15 工作日志

## AHF 英文产品页「图片布局」重做（模仿 CoEpower 图文排版）
- 背景：用户反馈"放图片的布局你并没有模仿好"——原页几乎无 `<img>`，只有 2 处 CSS 背景图。
- 文件：`src/pages/en/products/active-harmonic-filter.astro`
- 改动（均用项目现有图片 `public/assets/images/`）：
  1. Hero 改左文右产品主图分栏：`products-hero.webp`（加 Request a Quote / Learn More 按钮）。
  2. Overview 改左文右产品图 2 栏：`index-product-ahf.webp`。
  3. How It Works 5 步卡片下加 HMI 监控实拍图：`products-monitoring.webp` + 标题说明。
  4. Applications 由纯文字卡改为**行业图片网格**（6 张，绝对定位 + 渐变浮层标签）：datacenter/hospital/semiconductor/manufacturing/commercial-medical/renewable。
  5. 新增 `Case Studies` 图片区（id=cases）：`products-case-study-1/2.jpg` 2 图 + 说明。
  6. 新增 `Production & Quality` 图片区（id=production）：`services-process.jpg` + `about-global.webp` + `products-monitoring.webp` 3 图。
  7. 子导航补 `#cases`、`#production` 两个锚点。
- 复用图片映射注意：行业卡用 `products-industry-*.jpg` 与 `about-industry-*.webp`；Metals&Mining 暂用 `products-industry-semiconductor.jpg`（无专用矿冶图，后续可换）。
- 验证：dev(4321) 200；渲染 `<img>` 由 0 → 22（含 nav logo/flags）；各 img src、section id、subnav 锚点均就位。
- ⚠️ 旧提示仍有效：Why CHITEK 数字为推断值；相关链接 /products/static-var-generator、/products/sic-ahf-svg 仍 404；zh/es/ar/pt 未同步；build 需用户本地跑。

## sic-ultra-ahf 参数表样式收紧（2026-07-15）
- 需求："参数表左右距离边间距变小，行高变小，文字尽可能单行显示"。
- 文件：`src/pages/en/products/sic-ultra-ahf.astro` Technical Parameters 表（line 200–228，`id="parameters"`）。
- 改动（仅该 section，用 `_fix_param_table.py` + assert th==16/td==16）：
  1. 容器 `max-w-5xl px-6` → `max-w-6xl px-3`（表更宽、左右内缩更小）。⚠️ 该 `<div>` 在本文件出现 3 次（202/325/403），脚本按**行号**只改 202，勿全局 replace。
  2. th `text-left p-4 ...` → `... py-2.5 px-4 ... whitespace-nowrap`（16 处）。
  3. td `class="p-4 text-text-mid"` → `class="py-2.5 px-4 text-text-mid whitespace-nowrap"`（16 处）。
- 行高：p-4(16px 上下) → py-2.5(10px)，行高 ~53px → ~41px。
- 验证：dev 已重编译，served HTML td_new=16 / max-w-6xl px-3=1 / 旧类=0；Playwright 截图确认单行、更宽、更紧凑。
- ⚠️ 仅改 en 站；zh/es/ar/pt 同名页（若存在）未同步。
