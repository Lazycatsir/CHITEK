# 2026-09-16 工作日志

## 运营计划按"第一性原则"重写为 v2（用户批评 v1 没有侧重点）

- 用户指定四条主线：**① 清晰的网站结构 ② 关键词矩阵 ③ 产品页的塑造 ④ 数据分析工具 + 分析周报**；周期仍为 **3 个月**。
- 旧版 `docs/独立站运营计划.{md,html}` 已归档到 `F:\trash\chitek-plan-v1-20260916\`，新版同名重建。
- 产物：`F:\下载\CHITEK\docs\独立站运营计划.md` + `.html`（ASCII 副本：`C:\Users\lenovo\WorkBuddy\00_中转站\CHITEK-website-operations-plan.{md,html}`）。

### 本轮盘点出的关键事实（后续直接引用）
- **最大问题不是内容不够，而是存量不可被索引**：solutions.astro 里有 18 个场景 + 18 个案例（含治理前后数据），全是 JS 数组 + `link:'#'`；products.astro 里 8 个系列卖点挤在一页。
- **`products.astro` 的下载区块是假数据**：规格书 2.4MB / CE / UL / CAD / BIM / 视频共 8 项，`public/` 下**没有任何 pdf/dwg/zip/step/xlsx** → 访客点了没反应，信任事故。建议先改成「Send me the spec」跳表单，或等真文件。
- **services 与 solutions 不重叠**：services = 怎么合作（OEM/ODM/技术支持/经销），solutions = 用在哪（6 大行业）。之前担心重复，核实后排除。
- solutions 的 industries 结构：industrial / commercial / datacenter / grid / rail / renewable 六大类，各类 3 个子场景 = 18 场景，每个场景 3 痛点 + 1 案例。

### v2 计划的落点
- 结构目标：`/products`（Hub）+ 8 个产品详情页 + `/solutions/{6 个行业页}` + `/resources` + 新闻改 Content Collections；URL 五语同名。
- 关键词矩阵规则：**一个 URL 只打一组词**（避免 products 单页同时吃 AHF/SVG/SiC 导致每个词都排不进）。8 产品词群 + 6 场景词群已列出。
- 产品页模板：B2B 五问（是什么/参数/适合吗/有人用过吗/怎么买）+ 固定 9 段结构 + 4 个追踪钩子（data-product-name、/products/* URL、data-lead-form、下载链接）。
- 周报：只看 5 个数（询盘总数、有效询盘、来源 Top3、落地页 Top5、GSC 展现增长 Top10），周二取数避开 GA4 24–48h / GSC 2–3 天延迟。
- 卡点交给用户：真实参数表+型号表、可下载文件、案例授权。

### 工具备注
- Grep 全文搜 `\.pdf|\.dwg|\.zip|\.rar|\.step` 在含 node_modules 的工作区会 30s 超时 → 限定 path 或用 Glob `public/**/*.{pdf,...}`。
- PowerShell `Out-File` 捕获 Python stdout 会**产生中文乱码**（UTF-8 字节被按本地码解释）→ 数字/英文可用，中文内容需另存或直接读。

## 内链审计（用户要求"优先完善内链"，脚本 `_internal_link_audit.py`）

- 方法：扫 `src/pages/**/*.astro`，正则提取 `href="..."` 与 `lp('...')`，按语言归一化 URL（非 zh 页面里的裸 `/xxx` 视为 `/{lang}/xxx`），统计**正文内链**入链数。**Nav/Footer/Sidebar 是全局组件，不计入**（否则每个页面都显得有链接）。
- 结果：155 页，孤儿 16，**弱内链（入链≤1）121 = 78%**。
- **Nav 假阳性**：Nav 含 首页/products/solutions/services/about/news/contact + 语言切换 → 首页、services、contact 的"孤儿"不算真问题。
- **三个真问题**：
  1. **5 篇英文文章完全没入口**（列表页漏插卡片）：`ieee-519-harmonic-distortion-limits`、`sic-technology-redefines-ahf`、`svg-vs-traditional-capacitors`、`what-is-an-active-harmonic-filter`、`why-ev-charging-stations-need-ahf` → 等同死内容，**待修（P0）**。zh 的同类文章有卡片，说明是 en 列表页漏插。
  2. **唯一产品详情页 `/en/products/active-harmonic-filter` 零内链**（Nav 只链 `products#ahf` 锚点）→ **待修（P0）**。
  3. 120 篇新闻中 115 篇只有列表页 1 条入链，文章间互不引用 → 需 4 个支柱 Hub 页 + 正文上下文内链。
## 重要更正：初次内链审计有 bug，误报 5 篇英文文章为孤儿

- **根因**：列表页卡片写成 `<a href="{lp('/news/slug')}">`（JSX 表达式包在属性里），审计正则 `href="([^"]+)"` 抓到的是 `{lp('/news/xxx')}` 整串，以 `{` 开头 → `startswith('/')` 失败 → 被丢弃。
- **修正**：正则先尝试从属性值里再提取 `lp('...')` 内部路径，否则 strip 花括号。修正后：新闻文章孤儿 **0 个**（那 5 篇一直都有卡片）。
- **教训**：基于脚本结论要改站点前，先人工抽验 1-2 条。这次幸好在插入卡片前发现，否则会重复插入 5 张卡片。
- 修正后真实数据：155 页 / 孤儿 10（**全是 Nav 假阳性**）/ 弱内链 127（82%）。

## 已执行的修复（`_fix_product_links.py`，含 assert 计数）

1. **产品详情页内链**：`/en/products/active-harmonic-filter` 此前零内链（Nav 只链 `products#ahf`）。在 `en/products.astro` 的 AHF tab（data-tab="0"）标题段后加锚文本链接「Explore the full AHF series — specifications, models and applications」。
   - 坑：该段文案在 AHF/SVG 两个 tab 重复 → 必须用 `data-tab="0"` 定位后再找锚点，不能用 replace。
2. **4 条 404 死链**：products.astro 指向 `/solutions/datacenter`、`/healthcare`、`/semiconductor`、`/port`，**这 4 个行业页根本不存在**（`src/pages/en/solutions/` 无文件）→ 暂时改为 `/solutions`，待行业页建好后改回。
   - ⚠️ 这几个 URL 名（datacenter/healthcare/semiconductor/port）是计划里建行业页时应采用的**已存在链接目标**，不要另起 data-centers 之类的新命名。
- 验证：dev 重启后 `/en/products` 200，新链接存在，4 个死链全部消失；审计孤儿 11→10，产品页已不在孤儿清单。
- 环境坑：隔夜后 dev server 会停（请求返回 502），需重启并等 ~1 分钟；`_verify_products.py` 内置 wait_ready 轮询。

- 外链按用户给的分层框架纳入计划 §5.3；社媒补 §5.4（平台优先级 5:3:2 内容配比、帖子六段写法、红线）。

## 社媒建议要点（用户提问"社媒和外链有什么运营建议"）
- 定位：社媒不直接成交，作用是让客户搜索前就见过你 → **衡量只看 GA4 引荐会话与询盘，不看点赞**。
- 渠道：P0 LinkedIn（公司页 + **销售个人号**，个人号效果优于公司页）；P1 YouTube（视频嵌回产品页）；P2 Facebook/Instagram（中东拉美信任）+ Quora/Reddit/EEVblog（兼外链）；P3 X/微信。
- 内容配比 5:3:2 = 技术科普（复用 26 篇英文文章切片）/ 案例拆解（复用 18 个案例）/ 公司动态。
- 帖子六段模板：钩子（数字或痛点）→ 现象 → 原因 → 方案 → 数据 → CTA（链站内具体页面 + utm_source=linkedin，不要链首页）。
- 红线：不承诺效果（写"典型项目实测"）、不编案例/擅自公开客户名、不公开报价与交期。

## 用户对"外链"的现实反馈（重要，影响后续建议方式）
- 用户原话：「除了媒体网站，基本很难搞的；我就没见过非媒体网站有外链」→ 他的一线体感是**冷邮件索要外链成功率极低**，这是对的，不要再推"发邮件要链接"这套。
- 我的回应方向（已沟通）：① 认同冷邮件难；② 纠正"外链=合作伙伴页挂链接"的窄理解，真实来源包括代理商产品页、认证机构证书库、采购/中标公告、B2B 平台店铺（阿里国际站可在店铺放官网）、目录站；③ **更关键的判断：现阶段外链不是瓶颈**——产品页都还没建，外链指过来也没页面承接，外链是放大器不是发动机。
- 给的免费实操路径：Google 搜竞品品牌名并排除其官网（`竞品名 -site:竞品.com`），看同行实际出现在哪些非媒体网站 → 照着这些位置去占位；Ahrefs Webmaster Tools（免费）验证后可看自己站点现有外链。
- 后续提外链建议时：优先"业务过程中顺手留"（发货/交付/参展/媒体采访/认证更新时提供链接），不推冷邮件。

## 用户观察到"同行用子网站/副本站做外链"（PBN 站群）
- 已把 PBN/副本站/卫星站写进计划 §5.3 红线，并加了「合规多站点 vs 违规站群」对比表 + 识别指纹清单。
- 立场（已与用户沟通）：**明确不建议做**。理由——谷歌官方定义的作弊；SpamBrain 靠同 IP/同 whois/同模板/同 GA 代码/全部链接指向同一主站识别；惩罚连坐主站且延迟 6–18 个月；B2B 项目型客户会查供应商线上口碑，降权等于自拆询盘渠道。
- 提供给用户的识别方法：看那些副本站 6 个月后是否还在。
- 合规替代：多市场/多产品线**真实独立站**（如本站 5 语子域），有独立内容与流量，不做副本。：
当前收录期只铺枕链（DirectIndustry/Thomasnet/Kompass/Europages + LinkedIn/Quora/Reddit/Medium），月 5–10 条不爆发；锚文本 品牌词>URL>泛词>精准词；nofollow 占 25–35%；4–6 月上下游互链；6 月后 High DA 客座。验证闭环：Semrush 规划 / GSC 收录 / GA4 引荐+询盘定论。

## Bing Webmaster Tools 站点验证（用户提交 BingSiteAuth.xml）

- 验证 ID：`97E3493522EEB269E495058E6ECCE683`。
- 落地方式（双保险）：
  1. 文件验证：新建 `public/BingSiteAuth.xml`（build 后到站点根 `/BingSiteAuth.xml`）。
  2. meta 验证：`src/layouts/BaseLayout.astro` 的 `<head>` 加 `<meta name="msvalidate.01" ...>`（全语言生效，含 zh）。
- **踩坑：子域 force 通配会吞掉根目录静态文件**。`netlify.toml` 里 `https://en.chitek-inno.com/*` → `/en/:splat` 且 `force=true`，优先于静态文件，会把 `en.chitek-inno.com/BingSiteAuth.xml` 转到 `/en/BingSiteAuth.xml`（404）。已在该通配规则**之前**插入 8 条精确规则（en/es/ar/pt × www/非 www）→ `/BingSiteAuth.xml`。**Netlify 规则按顺序取首个匹配，新增规则必须前置。**
- 已同步进 `dist/`（脚本 `_patch_bing.py`）：copy xml + 向 90 个 html 注入 meta（patch=90，此前 0 命中）。netlify.toml 用 tomllib 校验通过，redirects 19 条。
- 备注：Bing 国内可访问，是 GSC/百度之外第三个收录来源；后续在 Bing WMT 提交 sitemap-index.xml。

## 产品页 SEO 结构改造 — 技术诊断（与运营计划 v2 §三「产品页的塑造」对齐）

代码层实地核对结论（后续执行直接引用，不必重查）：

- **全站产品详情页只有 1 个**：`src/pages/en/products/active-harmonic-filter.astro`。zh / es / ar / pt 均为 0 个。
- `src/pages/products.astro` 是 1777 行巨型单页。`matrixData`（2096 行）/ `specData`（2131 行）内联在 `<script>` 里，选型矩阵 `#matrix-tbody`、`#resource-list` **100% JS 渲染，DOM 初始为空** → 规格参数与文档库对爬虫完全不可见。这是产品页最值钱的内容，必须改成服务端渲染的静态 `<table>`。
- 系列键映射（来自 `seriesList`）：U=Ultra-Slim 超薄 / D=Distributed 分布式 / A=Anti-Pollution 抗污染 / C=SiC / L=Standard 标准型。电压档 208/400/480/690V，容量档 15/30/50/75/85/100/150/200A。
- `src/components/Nav.astro` 134–136、255–257 行：下拉指向 `/products#ahf` / `#svg` / `#sic` 锚点 → 不产生独立索引（内链审计已证实该详情页零入链）。
- `src/layouts/BaseLayout.astro` 38–43 行**无条件**输出 5 语 hreflang，但详情页只 en 存在 → 4 条 hreflang 指向 404，需改为按页面实际存在语言输出。
- 桌面/移动双 DOM 导致 H2 重复：「四大产品系列」「核心技术优势」「痛点与解决方案」「常见问题」各出现 2 次，稀释语义。
- 结构化数据仅 en 详情页有（BreadcrumbList + Product + FAQPage 三件套，`is:inline`），**可作为模板标杆反向补其他语言**。
- 落地路径：抽 `src/data/products.ts` → 规格表服务端静态渲染 → 建 `ProductLayout.astro` + Schema 组件 → 8 个产品页（ahf/svg × standard/sic/distributed/anti-pollution，URL 五语同名）→ Nav 改真实 URL → 老 `/products` 改造为 Hub。
- **红线**：规格数值不编造。只用可核实项（上述电压/容量/系列映射、IEC 61000 / IEEE 519 等通用标准），其余一律标「待技术部确认」，不填假数字（与「8 个下载资源是假数据」的教训同源）。

## 竞品分析：把用户给的 DTC 框架拓展成电能质量（AHF/SVG）工业版

用户发来一张「竞品分析新逻辑：看对手怎么赢」的框架图（4 支柱：市场位置→增长来源→成交放大→优势持续性），要求「拓展」。项目内此前**无任何竞品分析**（Grep `竞品|competitor` 只命中运营计划文档里的一处措辞）。

- 产物：`docs/竞品分析拓展手册.html`（13 节，72.9 KB）+ `docs/竞品分析-单竞品一页纸模板.md`（填空模板 + 横向对比矩阵）。
- ASCII 副本：`C:\Users\lenovo\WorkBuddy\00_中转站\competitor-analysis-playbook.html` / `competitor-onepager-template.md`。

### 本轮的核心判断（后续可直接引用）
- **原框架是 DTC/电商语言，直接套工业设备会有一半落不了地**。三个失灵点：① 漏斗不是「点击→下单」而是「触达→询盘→有效询盘→方案→报价→验厂→签约」（1–6 个月、3–7 个决策人）；② 转化不靠折扣/满减/bundle，靠「参数对得上 + 认证进得去 + 案例有人用过 + 交期售后敢写合同」；③ 无耗材无订阅，复购来自多站点复制 + 扩容 + SiC 换代 + 白标 OEM。
- 交付结构：**先给 DTC→B2B 翻译表，再把 4 支柱各自展开成「问题→去哪查→判断标准→产出」四层**，最后补竞品候选名单 + 市场事实底稿 + 一页纸模板 + 10 问自测。

### 检索到的行业事实（2026-09 检索，第三方估算，**未经我方实测，不得对外引用为事实**）
- 全球无功补偿与谐波治理市场 2025 年 USD 241.5 亿 → 2032 年 396.5 亿，CAGR 7.34%（口径含 SVC/STATCOM/被动/电容）。
- 有源滤波器占滤波器品类 38%（2026）；低压占电压段 66%；**50–200 A 容量段占 39% —— 主战场**。
- 「驱动器谐波治理」细分份额：ABB 24.4 / Schneider 21.6 / Siemens 16.7 / Eaton 15 / Danfoss 9.9 / Rockwell 8 / Mitsubishi 4.4（**口径是该细分，不是整个 AHF 市场**）。
- 2025–2026 动作线：2025-09 MTE 发 SyntriX AHF（SiC、>99% 效率、<50 µs、治至 50 次）；2025-12 Powerside PowerXAct；2026-04 Schneider 印度更新 EasyLogic APF/SVG；2026-05 YT Electric（上海英通）SiC AHF/SVG 全球预发布；2026-03 广交会中国厂商集中展示 AHF/SVG。
- 技术趋势四条：SiC 化 / 模块化紧凑（50A·100A 并联、壁挂机架）/ 单柜集成 AHF+SVG+DVR / 光伏电站低压 SVG（220–600 V）。
- 选型红线：AHF 按补偿电流 A 选、SVG 按 kvar 选，不可互换；两者都要 ≥20% 电流余量；纯电容在谐波母线会共振放大 5/7 次，必须配 7%/14% 调谐电抗器。
- 竞品候选名单（检索起点，未核实）：中国出海 — SINEXCEL 盛弘、YT Electric 上海英通、Xi'an CoEpower、ZDDQ 安徽中电、Lafaelt、Sieyuan 思源、RXPE 荣信汇科；国际平台商 — Schneider(AccuSine/EasyLogic)、Eaton(AccuSine+/HCU)、ABB、Hitachi Energy、Siemens；AHF 专家 — Danfoss(AAF 007)、MTE(SyntriX)、Comsys(ADF)、Powerside、TCI、Hammond、Merus Power；器件系 — TDK(PQSine)、MR(GRIDCON)、Arteche、Delta 台达、Nidec ASI。

### 工具备注（大段中文 HTML 的安全写法）
- **分块 `Write` → PowerShell .NET 合并**：把 HTML 拆成 5 个 part 写到 `.workbuddy/tmp/`，再用 `[System.IO.File]::ReadAllText(...,UTF8)` + `StringBuilder` + `WriteAllText(..., UTF8Encoding($false))` 合并（**不带 BOM**）。避免单次 Write 巨量中文的截断风险，也避免 Edit 对大 Unicode 块的静默截断。
- 合并后**用正则校验标签配对**：`<table>`/`</table>` 39/39、`<tr>` 291/291、`<t[dh]>`/`</t[dh]>` 897/897、结尾 `</html>`、13 个 `<h2 id=` 全在 → 72,981 bytes / 697 行，确认无截断。
- 本项目现有 HTML 交付风格：极简直白（1px 边框表格、无阴影/圆角/彩色 chip、`#0b4a7f` 单一强调色、`max-width:1080px`、含 `@media print`）。

## 竞品分析 · 第 2 份交付物：攻略记录（叙事版）

- 用户追加要求「把图片拓展补充成一篇攻略记录」→ 在《拓展手册》（词典式）之外，再出一份**叙事版** `docs/竞品分析攻略记录.html`（11 节、16.3 KB）。两者分工：手册=名词解释+模板（查），攻略=4 步动作+数字钉子+可背话术+自检清单（跑）。
- 内容：0 用途 / 1 三失灵点（类比讲）/ 2 四步总览表 / 第1步市场位置 / 第2步增长来源（6 引擎表）/ 第3步成交放大（钩子三类表+数字话术）/ 第4步优势持续性（四类壁垒表）/ 收口3判断 / 落到独立站4主线 / 红线5条 / 自检10问。含「数字钉子」：有源占滤波器品类38%、低压66%、50–200A占39%；MTE SyntriX >99%/<50µs 作真壁垒刻度。
- ASCII 副本：`C:\Users\lenovo\WorkBuddy\00_中转站\competitor-playbook-record.html`。安全写法同手册（分块 Write→PowerShell 合并→正则校验：6 表/11 h2/结尾</html> 全对）。

## 全站 favicon（此前 head 无任何 icon 声明，浏览器显示默认地球）

- BaseLayout.astro head 原来完全没有 `<link rel="icon">`。logo.webp 是 1200×275 横版字标，直接缩会糊 → 用 `_gen_favicon.py`（PIL）裁出**主行第一个 "C" 字标**生成方形 favicon。
- 生成物：`public/assets/images/favicon-32.png / favicon-64.png / apple-touch-icon.png`（黑底橙 C，与 logo 一致）。脚本可重跑（列扫描+行分带自动定位 C；**坑**：必须先按行分带只取 CHITEK 主行，否则会把下排 INNOPOWER 的 I/N 一起裁进来）。
- BaseLayout head 新增 3 行声明（32/64/apple-touch），全站 5 语言生效。dev 验证：3 个 png 200，页面 head favicon 链接计数 2+apple-touch。
- 部署后浏览器 tab 即显示 C 字标；注意 favicon 有缓存，看不到就强刷或换隐身窗。

## favicon 改用官方 SVG 字标（用户给 logo-head.svg 替换裁出的 C）

- 用户提供了 `F:\下载\logo-head.svg`：119×119 viewBox、橙红 `rgb(255,82,0)` 的居中 "C" 字标（透明背景），比从长条 logo 裁的 C 更规整。
- 已复制为 `public/assets/images/logo-head.svg`，并改为**主 favicon 直接用 SVG**（`BaseLayout` head：`<link rel="icon" href="/assets/images/logo-head.svg" type="image/svg+xml" sizes="any">` 置顶）→ Chrome/Edge/Firefox 原生支持 SVG favicon，正好是干净 C 字标。
- 保留原 PNG（favicon-32/64/apple-touch-icon）作 Safari/iOS 兜底（仍是橙色 C，旧版带黑底）。
- 环境无 cairosvg、CDP 浏览器返回 502，故未把 SVG 栅格化成新 PNG（沿用旧 PNG 兜底足够）。dev 验证：logo-head.svg 200、head 含 svg 图标声明。
- 临时脚本（`_check_cairosvg.py`/`_gen_favicon_svg.py`/`_raster_result.txt`）已移至 `F:\trash\favicon-svg-cleanup-*`。

## 竞品分析 · 第 3 问：科普文占首位 + SERP 三段字体来源（用户带 2 张截图）

- 用户问题二部分：① 对手靠写科普文（popular-science/教育型长文）占搜索首位，怎么运作、CHITEK 怎么反击；② 一条 Google 结果里「三段字体」分别来自哪（用户已知中间是原文）。
- 结论（已交付 `docs/科普文SEO反击清单.html` + ASCII 副本 `C:\Users\lenovo\WorkBuddy\00_中转站\science-content-seo-counterplan.html`）：
  - SERP 三段：顶部蓝标题 ← `<title>`（Google 改写约 6–7 成，优先级 title→H1→og:title→锚文本→WebSite schema）；**中间站点名+面包屑 ← WebSite 结构化数据/og:site_name/BreadcrumbList，基本原样显示（即用户认的「原文」）**；底部黑摘要 ← meta description（无则回退正文）。
  - 科普文逻辑 = 内容资产打「触达→询盘」漏斗顶端，且被 AI Overviews 引用（引用 2–6 源、约 73% 来自自然前 10）→ 双曝光吸点击。CHITEK 反击 7 步：建科普内容簇(pillar+cluster)、补 FAQPage/BreadcrumbList/WebSite schema、抢 AI Overview 引用(事实密度+对比表+Organization+作者权威)、多语言(en/es/ar 攻 Google、zh 攻百度)、内链接权(用 _internal_link_audit.py 导到 products/solutions)、真实草稿优先(web-crawler/outreach/templates)、GSC/GA4 看自然流量与 generate_lead。
  - 红线：不编案例、不承诺排名/效果、第三方数据不对外引用为事实。

## en/products.astro 中间内容替换为 ytelect AHF 列表（用户指令）

- 指令：把 en/products.astro「hero 与底部栏之间的内容」改成参考文件 `F:\projects\cloned-sites\ytelect-apf\www.ytelect.com\active-power-filter_c3.html` 的内容，保留原侧边栏；明确「不包括 hero 与底部栏」。
- 处理：保留 hero(24–40)、`</BaseLayout>`(footer/sidebar 在此)、trailing `<script>`；仅替换原 product-series 区块(43–1782) 为 ytelect 派生的 AHF 分类列表 = 面包屑 + 分类介绍段 + **12 个产品卡**。
- 适配：图片→本地占位 `/assets/images/index-product-ahf.webp`；所有外链→`#`；品牌 YTPQC→CHITEK（描述沿用 ytelect 通用 AHF 文案，未编造参数）。
- 实现：托管 Python 路径不存在（C:\Users\lenovo\.workbuddy\binaries\python\versions\3.13.12\python.exe 实测无此文件），改用 PowerShell `System.IO.File` 按行索引拼接（head 0..40 + new + tail 1782..）。临时文件 new_section.html / scripts_replace_products_section.py / sp_log* 已移至 F:\trash。
- ⚠️ 残留：原 sticky-tab-bar(AHF/SVG 双按钮)在 hero 之上、不在替换范围，现按钮已无对应 panel 可切换，属死 UI。后续要么删掉，要么改成单分类（当前页是纯 AHF 分类列表）。


## en/products.astro 验证渲染（13:57 续）
- 用托管 node 直跑 astro dev，4321 被上轮遗留 server 占用，落到 4322（astro v6.3.1 ready）。
- 抓取 http://localhost:4322/en/products 校验：STATUS 200；index_main=1、n_left=1、n_right=1、cbp-vm-view-grid=5、View More=12、cbp-tag=38、cbp-vm-li=20(含 CSS)。
- 确认 ytelect 两栏 + 网格/列表切换结构正确渲染。预览地址 http://localhost:4322/en/products。
- 注：4321 旧 server 仍在，下次若需 4321 需先 taskkill 旧进程。

## 产品页/configurator 四项改动（用户指令收口）

- **en/products.astro**（脚本 `_fix_en_products.py` 落地，备份 `.bak_enproducts2.astro`）：
  1. 删分类介绍灰段（"CHITEK AHF based on 3-level topology…"）。
  2. 删 12 张卡片灰色描述 `<p class="cbp-vm-details">`（CSS 类定义 `.cbp-vm-details` 保留无害）。
  3. Contact Us 底部 textarea(`data-inquiry-summary`) + Inquire Now 按钮**移入 Filter 卡片底部**（`<div class="mt-4 pt-4 border-t border-gray-200">` 分隔），contact_card 只剩 WhatsApp + 邮件。
  - 注意：原脚本首次跑因 B2 断言在 C 前异常中断（PowerShell 报 exit 0 假成功），文件未写；重写为带 try/except 逐步日志的版本后成功。data-inquiry-summary 现 2 处 = 注入的 textarea + 筛选 JS `querySelector('[data-inquiry-summary]')`。
- **en/products/sic-ultra-ahf.astro**（脚本 `_fix_sic_v2.py` 落地，备份 `.bak7.astro`）：
  1. Connection 与 Installation 改为**同一行、顶部对齐**（`flex flex-wrap items-start gap-x-8` 包两个 label 组）。
  2. 自动填充 JS 由多行 `Voltage:` 格式**改为 SKU 单行**：`CHITEK-SiC-AHF-{a}-{v}-{i} · 500 × 550 × {d}mm · {kg}kg`，SPECS 按电流给深/重，DEF=`{v:'400V',a:'100A',i:'Rack'}`（默认 `CHITEK-SiC-AHF-100A-400V-Rack · 500 × 550 × 135 mm · 27 kg`）。Download 文件名 `CHITEK-SiC-AHF-configuration.txt`。
  3. 删 Sizing(03)、Performance(05)、Get A Quote Now CTA 三块 + 对应 4 条 nav 链接（sidebar+mobile）→ 剩 7 分节：01 Parameters/02 Solutions/04 Features/06 Applications/07 FAQ/08 Why CHITEK/09 Delivery。
  - ⚠️ **SKU 前缀歧义**：用户给的格式例写 `CHITEK-AHF-`、默认值例写 `CHITEK-Sic-AHF-`，取与下载文件名一致的 `CHITEK-SiC-AHF-`；若改 `AHF-` 只动 `sic-ultra-ahf.astro:476` 一行。
- dev server 用户手动 kill（背景任务 rq18jj 取消），本轮未重启、未做 served-HTML 探针（静态核对 + 结构读已确认）。预览需用户自起 `astro.mjs dev --host --port 4321`。
- 待办（跨语言）：zh/es/ar/pt 兄弟页未同步上述改动；contact 表单 Netlify Forms 未注册（POST 404，需部署后生效）。