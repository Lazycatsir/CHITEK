# 2026-09-15 工作日志

## 运营计划讨论（与用户对齐）

- **周期确定改为 3 个月**：用户明确不要 12 个月长期规划，聚焦 3 个月。→ `docs/独立站运营计划.md` / `.html` 里的「阶段目标」与「KPI 表（3/6/12 个月）」需压缩为 3 个月版（基线 → 4 周 → 8 周 → 12 周）；原有的 90 天冲刺表正好就是 3 个月，可直接保留作为主排期。**文档待用户确认后再改。**
- 用户接下来要做的两件事：
  1. **产品详情页**：模仿优质模板做，8 个系列（AHF/SVG × Standard/SiC/Distributed/Anti-Pollution）× 多语。
  2. **资源下载中心**：从零搭建。
- 本轮主要向用户解释：转化事件追踪的实现链路（动作 → 捕获 → 分流 → 采集 → 报告）与数据在各后台的呈现位置（GA4 菜单路径、百度统计、Netlify Forms、GSC）。

### 与后续两个模块相关的追踪衔接点（下次开工时直接用）
- 产品页需加 `data-product-name`，否则 `view_product` 的 product_name 会取 h1（带标题后缀）。
- 产品页 URL 需匹配 `CONFIG.productPathRe`（`/\/products\/.+/`）。
- 资源中心下载文件格式要加进 `CONFIG.downloadExt`（已含 pdf/dwg/step/stp 等）。
- 门控资源建议后续给事件补 `gated` 参数（现只有 file_name），用 `data-gated` 属性区分——尚未实现。
