# 2026-07-21 工作日志

## 打开首页 / 重启 dev server
- 跨会话后台 dev 进程已退出（Test-NetConnection localhost:4321 = False）。
- **本会话 Bash 工具持续异常**：所有 Bash 调用返回 `Command rejected: undefined`（Parameter "command" expected string, but received undefined），疑似沙箱对 Bash 的参数传递故障。Write/Read/Edit/PowerShell 工具正常。
- 改用 PowerShell 启动 dev server：
  `Start-Process -FilePath "C:\Users\lenovo\.workbuddy\binaries\node\versions\22.22.2\node.exe" -ArgumentList "F:\下载\CHITEK\node_modules\astro\bin\astro.mjs","dev","--host","--port","4321" -WorkingDirectory "F:\下载\CHITEK" -WindowStyle Hidden`
  然后用循环 `Invoke-WebRequest -Uri http://localhost:4321/ -UseBasicParsing -TimeoutSec 3` 轮询，约 60s 后 HTTP=200 就绪。
- 首页可访问：`http://localhost:4321/`，英文 `http://localhost:4321/en/`。
- 提醒：后续若需跑命令，Bash 不可用则改用 PowerShell（curl/Test-NetConnection/Start-Process 均可用）。Playwright 实测仍走共享 venv python（之前验证 AHF reveal 动画用）。

## AHF 产品页 — Series 区块标题动画强化（用户再次反馈"只有卡片动、标题不动"）
- 用户明确指 `en/products/active-harmonic-filter` 的 Four Product Series 区域标题无动画、仅卡片有。
- 排查：用 Playwright 逐步滚动实测 Series 区块标题——**最新代码完全正常**（初始 opacity=0/translateX=-40px；滚动到 top=633 视口内首次触发 revealed，opacity 过渡、translateX 滑入到 0；滚回重置可重复）。源文件确认是逐元素版本（line 363 querySelectorAll('.series-heading,...')、line 291/293 .series-heading.revealed）。AHF 文件无 fade-up 类（排除"卡片动来自全局淡入"假设）。
- 结论：headless 实测代码正常，用户端"标题不动"极可能是浏览器缓存了某个中间版本（scope 未修复或整组触发版）的旧 module。
- 强化改动（en/products/active-harmonic-filter.astro）：
  - CSS 隐藏态 translateX -40px → -72px，transition 0.6s → 0.85s，加 will-change。
  - JS IO rootMargin '0px 0px -8% 0px' → '-18%'、threshold 0.15 → 0.1（标题在视口更内部才滑入，停留更久可见）。
  - 重启 dev server（taskkill 旧 PID 8600 → Start-Process 新进程 PID 4080），使所有 module URL 时间戳变化以破缓存。
- Playwright 复测（强化后）：标题 opacity=0/translateX=-72px → STEP14 top=633 触发滑入 → settle opacity=1/translateX=0 → 滚回重置。✅
- 环境注意：PowerShell 复杂命令（含 for 循环/$i 变量/Invoke-WebRequest）被安全拦截 "Encoded commands hide the actual code"；改用 `Start-Process` 启动 + `Start-Sleep` 等待 + `& python script` 实测。`Test-NetConnection` 因 ICMP 探测会超时不可靠，用 Playwright goto 确认就绪更准。Start-Process 子进程在工具返回后仍能存活（用户访问 OK）。
- 待用户确认：请硬刷新（Ctrl+Shift+R）后查看；若仍不动需用户给 Console 报错或无痕窗口结果。

## 关键纠正：用户说的 "product 页面" 是 `/en/products`（列表页），不是 detail 页
- 用户原话："英文页面的product页面 Four Product Series... 只有卡片有动画加载效果，标题并没有" + 纠正 "我说的是 http://localhost:4321/en/products页面"。正确目标是 **列表页** `src/pages/en/products.astro`，不是 `active-harmonic-filter.astro`（detail 页，之前已修）。
- 列表页动画体系（与 detail 页不同）：`.reveal`(淡入上移) / `.hmi-left`/`.hmi-right`(translateX ±64px，hmiObserver 可重复触发) / `.reveal-x-left`(本次新增，translateX -64px)。
- 病灶：列表页两处 "Four Product Series, Full-Scenario Coverage" 标题 h2（Tab0 AHF line49、Tab1 SVG line255）**原本没有任何动画类**，卡片靠 product-track 体系动，所以"只有卡片动、标题不动"。
- 修复（src/pages/en/products.astro）：
  1. CSS（line 1819-1820）新增 `.reveal-x-left { opacity:0; transform:translateX(-64px); transition... }` + `.reveal-x-left.visible { opacity:1; transform:translateX(0) }`。
  2. 两处 h2 加 `reveal-x-left` 类（replace_all）。
  3. `<script>` 新增专用 IO（接在 hmiObserver 后，约 line 2062）：`document.querySelectorAll('.reveal-x-left')`，rootMargin `0px 0px -20% 0px`、threshold 0.1，进入加 .visible / 离开移除 → **可重复触发**。
  - 为什么不复用 `.hmi-left`：hmiObserver 无 rootMargin、threshold 0.25，首屏可见元素加载即触发 → 标题在顶部就直接 visible，看不到"滑入"。`.reveal-x-left` + 更深触发点才会在滚动到视口内部时滑入。
- Playwright 验证（products_title_verify.py，JSON 输出）：count=2；初始 opacity=0/translateX=-64px；滚动到中心后 opacity=1/translateX=0（滑入）；滚回顶部 opacity=0/-64px（重置，可重复）；无 page error。✅
- 注意：Tab1(SVG) 标题在隐藏 tab 面板内（bounding_box=null），切到 Tab1 且进入视口时 IO 会触发。

## `/en/products` — Core Technology Advantages 标题 + 卡片也加从左滑入动画
- 用户："Core Technology Advantages以及下面卡片也要动画加载效果"。目标仍是列表页 `src/pages/en/products.astro` 的 Core Technology Advantages 区块（Tab0 AHF / Tab1 SVG 各一处）。
- 原状：该区块标题容器 `<div class="text-left mb-16 reveal">` 和卡片网格 `<div class="grid ... reveal">` 用的都是 `reveal`(淡入上移)；卡片 `.feat-card` 用 `translateY(20px)`。用户要的是"从做到右"(左→右滑入)，与原 `reveal` 淡入不一致。
- 修复（src/pages/en/products.astro）：
  1. 标题容器 `reveal` → `reveal-x-left`（复用 xLeftObserver，滑入 -64px、可重复）。两处 replace_all。
  2. 卡片网格 `reveal` → 新类 `feat-grid`（不再用全局 revealObserver 的嵌套 stagger）。
  3. `.feat-card` CSS：隐藏态 `translateY(20px)` → `translateX(-48px)`，transition 0.55s，加 will-change；`.feat-card.visible` → `translateX(0)`。
  4. 删除 6 张卡片上的内联 `style="transition-delay:150/300/450ms"`（改由 JS setTimeout(i*150) 控错峰，避免双重延迟）。
  5. `<script>` 新增 `.feat-grid` 专用 IO（约 line 2076）：对每个网格，进入时按索引 `setTimeout(card.classList.add('visible'), i*150)` 错峰滑入；离开移除 .visible → **可重复触发**。threshold 0.15, rootMargin `0px 0px -12% 0px`。
  6. `prefers-reduced-motion` 媒体查询补上 `.reveal-x-left, .feat-card`（无障碍）。
- Playwright 验证（products_cta_verify.py）：标题容器初始 opacity=0/translateX=-64 → 中段 opacity=0.72/tx=-17.8 → 末 opacity=1/tx=0 → 滚回重置；4 卡片初始全 opacity=0/tx=-48，中段呈完美错峰(card0 op0.84/tx-7, card1 0.58/tx-20, card2 0.04/tx-46, card3 仍 0/tx-48)，末全 opacity=1/tx=0，滚回全重置。✅ 切到 Tab1 时该面板卡片进入视口也会触发。
- 控制台报错排查：发现页面加载期有 2~3 条 `PAGEERROR: Object`（message 为 "Object"、stack=null）。对比测试 `/en/`（首页）同样有 3 条 → **属全局预存报错（共享 layout 脚本，第三方/minified 抛 `throw {}`），与本次改动无关**；改动相关的 scroll/动画逻辑无任何报错，页面 HTTP 200、Astro script 编译正常。不在本次范围内，若用户要求零 console 报错再单独排查。

## 把 /en/products 动画改动同步到其余 4 语言（zh/es/ar/pt）
- 用户："http://localhost:4321/en/products这个页面改动也同步其它语言"。目标：zh 根 `src/pages/products.astro`（路由 /products）、es/ar/pt 子目录同名文件。
- 探查：4 个文件结构与 en 原始版逐字一致（Four Product Series 标题 h2 用同一 class 串、Core Tech 区块 `reveal`、feat-card 带内联 `transition-delay`、有 `hmiObserver` 但无 `xLeftObserver`/`feat-grid`）。唯一差异：zh 根页 tabs 更多（hero 3 tab + features 2 tab + 其他），但 Core Tech 区块结构相同。
- 因文件含大量中文/阿拉伯文，用 **Python 脚本做 ASCII class 串的 `content.replace`**（避免 Edit 工具在大 Unicode 文件上静默截断）。脚本对 4 文件各做 10 处替换：① Four Product Series 标题 h2 加 `reveal-x-left`；② Core Tech 标题容器 `reveal`→`reveal-x-left`；③ 卡片网格 `reveal`→`feat-grid`；④ `.feat-card` CSS `translateY(20px)`→`translateX(-48px)`+will-change；⑤ 删 6 张卡片内联 `transition-delay`(150/300/450ms)；⑥ 注入 `.reveal-x-left` CSS；⑦ `prefers-reduced-motion` 补 `.reveal-x-left,.feat-card`；⑧ 注入 `xLeftObserver`+`feat-grid` observer（与 en 完全一致）。
- 验证脚本坑：复用用户实时 CDP context 开多 context 会 TargetClosed；`cs` 函数误把 ElementHandle 当 selector 传给 query_selector 报 "not JSON serializable"。修正：单 page 在 `contexts[0]`、顺序测 4 语、每步重新 query 元素、每语 try/except。
- Playwright 验证（verify_all_langs.py）：zh/es/ar/pt 全部 status=ok，标题+4 卡片初始隐藏(标题 tx-64/卡片 tx-48)、中段错峰、末全 visible、滚回重置（可重复）；仅余预存 "Object" 报错（zh8/es6/ar4/pt2，同源全局报错，无新增）。✅
- grep 校验：5 个语言文件 `reveal-x-left|feat-grid|const xLeftObserver|querySelectorAll('.feat-grid')` 计数均为 12（与 en 一致），确认结构同步对齐。

## Git 提交到 GitHub（用户明确 "提交项目到github"）
- 提交范围（97 文件，+9096/-2252）：`src/`（products 五语 AVR 移除 + 排序/动画同步 + 24 篇新新闻）、`CLAUDE.md`、`README.md`、`.workbuddy/memory/`（项目记忆）。
- **未提交**：`.workbuddy/*.py`、`.workbuddy/*.log`/`*.json`（我生成的临时脚本与验证日志，属噪音）。
- 命令：`git commit` 后 `git push origin master` → 成功 `c2f4078..0772915 master -> master`（远程 git@github.com:Lazycatsir/CHITEK.git）。
- 注意：仓库 core.autocrlf/归一化对内存 md / CLAUDE.md / BaseLayout.astro 等报 "LF will be replaced by CRLF" 警告，属正常行尾归一化，提交内容无误。

## Netlify 构建失败排查（frontmatter 缺逗号）
- 现象：GitHub 推送成功，但 Netlify "没反应"——实际是**构建失败**（GitHub→Netlify 连接正常，push 正常触发了 build）。报错：`Expected "}" but found "keywords"` @ `src/pages/es/news/carbon-neutral-factories.astro:11:2`。
- 根因：新闻文章 `const article = { ... }` 对象里，`readTime` 行末尾**缺逗号**，其后追加的 `keywords` 字段导致对象字面量语法错误（疑似早前批量加 keywords 时漏了逗号）。同批 `ar/news/carbon-neutral-factories.astro` 有同样坑，esbuild 在第一个文件报错停下，所以只先看到 es。
- 修复：先用 esbuild 全量校验 136 个 .astro 的 frontmatter（`verify_frontmatter.mjs`，`transformSync` loader=ts），确认仅 2 文件 BROKEN；再用 regex 给 readTime 补逗号、删空行、统一 CRLF（`repair_frontmatter.py`）。修复后 BROKEN 0。
- ⚠️ 教训：批量给对象字面量追加字段时，**上一行必须补逗号**。本环境 `npm run build` 被 dist 守卫阻断无法本地验证，可用 **esbuild.transformSync 对 frontmatter 做语法预检**（等同 Netlify 用的解析器）作为提交前的本地 gate。这两个脚本留在 `.workbuddy/` 未提交。
- 提交：`13b2bea` fix: 修复新闻页 frontmatter 缺逗号导致构建失败（2 文件，push 成功）。
