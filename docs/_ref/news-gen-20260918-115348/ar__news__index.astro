---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Sidebar from '../../../components/Sidebar.astro';
import NewsCard from '../../../components/news/NewsCard.astro';
import NewsPager from '../../../components/news/NewsPager.astro';
import { arNewsCards, NEWS_PER_PAGE, NEWS_TOTAL_PAGES } from '../../../data/arNewsCards.js';

const isLocal = /localhost|127\.0\.0\.1|0\.0\.0\.0/.test(Astro.url.host);
const lp = (p: string) => isLocal ? '/ar' + p : p;

const current = 1;
const total = NEWS_TOTAL_PAGES;
const cards = arNewsCards.slice(0, NEWS_PER_PAGE);
const pageUrl = (n: number) => (n === 1 ? lp('/news/') : lp('/news/page_' + n + '/'));
---
<BaseLayout title="الأخبار والرؤى | CHITEK INNOPOWER" description="تابع آخر الأخبار والورقات التقنية ورؤى الصناعة من CHITEK INNOPOWER لحلول جودة الطاقة." lang="ar" alternateLangs={['zh', 'en', 'es', 'ar']}>
  <Sidebar lang="ar" />

  <!-- Hero Section -->
  <section class="relative w-full overflow-hidden min-h-[33vh] mt-[72px] flex items-center bg-bg-dark text-white">
    <div class="absolute inset-0 opacity-10">
      <div class="absolute inset-0 bg-gradient-to-br from-brand-orange/20 to-transparent"></div>
    </div>
    <div class="relative z-10 w-full max-w-7xl mx-auto px-5 md:px-12">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold font-[family-name:var(--font-barlow-condensed)] tracking-tight mb-6">
        الأخبار والـ<span class="text-brand-orange">رؤى</span>
      </h1>
      <p class="text-lg md:text-xl text-gray-300 max-w-2xl leading-relaxed">
        تابع أحدث التطورات في تكنولوجيا جودة الطاقة واتجاهات الصناعة وحلول CHITEK.
      </p>
    </div>
  </section>

  <!-- Latest Articles -->
  <section id="articles" class="pt-10 md:pt-14 pb-16 md:pb-20 bg-white">
    <div class="max-w-7xl mx-auto px-5 md:px-12">
      <div class="space-y-6">
        {cards.map((card) => (
          <NewsCard
            href={lp(card.href)}
            img={card.img}
            alt={card.alt}
            category={card.category}
            cat={card.cat}
            date={card.date}
            title={card.title}
            desc={card.desc}
            readMore="اقرأ المزيد"
          />
        ))}
      </div>

      <NewsPager
        current={current}
        total={total}
        url={pageUrl}
        label="ترقيم صفحات الأخبار"
        prevLabel="الصفحة السابقة"
        nextLabel="الالصفحة التالية"
      />
    </div>
  </section>
</BaseLayout>
